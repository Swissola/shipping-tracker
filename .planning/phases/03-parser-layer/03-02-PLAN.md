---
phase: 03-parser-layer
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - shipping_tracker/parsers/aliexpress.py
  - shipping_tracker/parsers/__init__.py
autonomous: true
requirements: [PARSE-02]
must_haves:
  truths:
    - "AliExpressParser.can_parse() returns True for declared AliExpress sender domains and False otherwise (D-01)"
    - "extract() returns a TrackingInfo with the correct tracking number for label-anchored bodies (D-02 primary path)"
    - "extract() returns a TrackingInfo via the shape-pattern fallback when no label is present (D-02 fallback)"
    - "extract() returns None for pre-shipment bodies with no tracking number (D-05)"
    - "carrier is None when the email does not name a courier (D-04); the parser never logs body/sender/tracking number (LOG-02)"
  artifacts:
    - path: "shipping_tracker/parsers/aliexpress.py"
      provides: "AliExpressParser + ALIEXPRESS_SENDER_DOMAINS constant + compiled label/shape regexes"
      contains: "class AliExpressParser(BaseParser)"
      min_lines: 30
    - path: "shipping_tracker/parsers/__init__.py"
      provides: "Re-export of AliExpressParser and ALIEXPRESS_SENDER_DOMAINS"
      contains: "AliExpressParser"
  key_links:
    - from: "shipping_tracker/parsers/aliexpress.py"
      to: "shipping_tracker/parsers/base.py"
      via: "from shipping_tracker.parsers.base import BaseParser, TrackingInfo"
      pattern: "from shipping_tracker.parsers.base import"
    - from: "shipping_tracker/parsers/aliexpress.py"
      to: "ALIEXPRESS_SENDER_DOMAINS"
      via: "can_parse() membership test feeding D-01 sender wiring"
      pattern: "ALIEXPRESS_SENDER_DOMAINS"
---

<objective>
Implement `AliExpressParser` — the self-contained parser module that owns its sender-domain
constant (D-01), matches on sender domain (`can_parse`), and extracts the tracking number via
the label-anchored-with-shape-fallback strategy (D-02), returning `TrackingInfo | None` so the
pre-shipment case is non-fatal (D-05). This is the vertical-slice core: it makes the can_parse
and extract unit tests from Plan 01 go GREEN.

Purpose: Deliver PARSE-02 — a correct, mypy-strict, PII-safe AliExpress parser that is a
drop-in module (one file, its own sender constant) so adding a future seller needs no central
edit.
Output: New `shipping_tracker/parsers/aliexpress.py`; updated `parsers/__init__.py` re-export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-parser-layer/03-CONTEXT.md
@.planning/phases/03-parser-layer/03-RESEARCH.md
@.planning/phases/03-parser-layer/03-PATTERNS.md
@.planning/phases/03-parser-layer/03-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement AliExpressParser (can_parse + extract) and re-export it</name>
  <files>shipping_tracker/parsers/aliexpress.py, shipping_tracker/parsers/__init__.py</files>
  <behavior>
    - can_parse("", "shipping@mail.aliexpress.com") is True; can_parse("", FAKE_OTHER_SENDER) is False (D-01)
    - extract(FAKE_ALIEXPRESS_SHIPPED_BODY).tracking_number == "FAKELP00FAKE00001" (label-anchored, D-02 primary)
    - extract(FAKE_ALIEXPRESS_NOLABEL_BODY) returns a TrackingInfo via the shape fallback (D-02 fallback)
    - extract(FAKE_ALIEXPRESS_PRESHIPMENT_BODY) is None (D-05); the numeric order reference is NOT matched
    - extract(...).carrier is None for the shipped-body fixture (D-04 best-effort, not extracted in v1)
    - No log record emitted by the parser contains body text, sender, or the tracking number (LOG-02)
  </behavior>
  <read_first>
    - shipping_tracker/parsers/base.py (the ABC contract just edited in Plan 01 — extract() -> TrackingInfo | None, TrackingInfo with optional carrier)
    - shipping_tracker/gmail/client.py (logging pattern to copy: module-level `logger = logging.getLogger(__name__)`, `%`-style log calls, message_id only)
    - shipping_tracker/parsers/__init__.py (current re-export surface — append AliExpressParser + constant)
    - .planning/phases/03-parser-layer/03-PATTERNS.md §`shipping_tracker/parsers/aliexpress.py` (exact imports, ALIEXPRESS_SENDER_DOMAINS value, the two compiled regexes `_LABEL_RE`/`_SHAPE_RE`, and the can_parse/extract bodies)
    - .planning/phases/03-parser-layer/03-RESEARCH.md §AliExpress Email Specifics (label strings, sender domains, shape families) and §Common Pitfalls (1, 2, 3)
    - tests/test_aliexpress_parser.py + tests/fixtures/fake_aliexpress_email.py (the failing tests + fixture values this implementation must satisfy — created in Plan 01)
  </read_first>
  <action>
    Create shipping_tracker/parsers/aliexpress.py following the PATTERNS.md sketch exactly:
    module docstring; `from __future__ import annotations`; stdlib imports `logging`, `re`;
    project import `from shipping_tracker.parsers.base import BaseParser, TrackingInfo`;
    `logger = logging.getLogger(__name__)`. Declare the parser-owned constant
    `ALIEXPRESS_SENDER_DOMAINS: tuple[str, ...] = ("@mail.aliexpress.com", "@aliexpress.com")`
    (D-01 single source of truth). Compile two module-level regexes once at import time (never
    inside extract()): `_LABEL_RE` (case-insensitive VERBOSE label-anchored pattern capturing
    `[A-Z0-9]{8,35}` after the known labels Tracking number / Tracking No. / Logistics No. /
    Logistics tracking number / Waybill / Parcel) and `_SHAPE_RE` (VERBOSE fallback whose every
    alternative carries a mandatory letter component — LP\d{10,16}, [A-Z]{2}\d{8,10}[A-Z]{2},
    YT\d{14,18}, [A-Z]{2}\d{9,13}[A-Z]{2} — so purely-numeric order references cannot match, per
    D-02 and RESEARCH.md Pitfall 2). Implement `can_parse(self, email_body, sender) -> bool` as
    `any(domain in sender for domain in ALIEXPRESS_SENDER_DOMAINS)`. Implement
    `extract(self, email_body) -> TrackingInfo | None`: try `_LABEL_RE.search` first
    (return TrackingInfo(tracking_number=group(1))), then `_SHAPE_RE.search`
    (return TrackingInfo(tracking_number=group(0))), else return None. Do not set carrier
    (leave the default None — D-04). Do NOT log inside extract(); message_id is unavailable here
    (the dispatch loop in Plan 03 owns no-tracking/no-match logging). In parsers/__init__.py,
    re-export AliExpressParser and ALIEXPRESS_SENDER_DOMAINS. Regexes use only bounded
    quantifiers — no nested unbounded quantifiers (ReDoS-safe, RESEARCH.md Security Domain).
    No fenced code in this action — replicate the exact patterns from 03-PATTERNS.md.
  </action>
  <verify>
    <automated>python -m mypy shipping_tracker/parsers/aliexpress.py</automated>
    <automated>pytest tests/test_aliexpress_parser.py::test_can_parse_known_domains tests/test_aliexpress_parser.py::test_can_parse_rejects_other_senders tests/test_aliexpress_parser.py::test_extract_label_anchored tests/test_aliexpress_parser.py::test_extract_shape_fallback tests/test_aliexpress_parser.py::test_extract_returns_none_preshipment tests/test_aliexpress_parser.py::test_extract_carrier_none tests/test_aliexpress_parser.py::test_extract_does_not_log_pii -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m mypy shipping_tracker/parsers/aliexpress.py` reports zero errors (mypy --strict)
    - The 7 parser-unit tests listed in the verify command all pass (PARSE-02 coverage from 03-VALIDATION.md)
    - `grep -c "re.compile" shipping_tracker/parsers/aliexpress.py` is 2 (both regexes compiled at module level, not inside extract)
    - `grep -E "logger\.(debug|info|warning|error)" shipping_tracker/parsers/aliexpress.py` returns nothing referencing body/sender/tracking_number (LOG-02 — ideally no log calls inside extract at all)
    - extract(FAKE_ALIEXPRESS_PRESHIPMENT_BODY) returns None — the numeric order reference is not captured (RESEARCH.md Pitfall 2)
    - AliExpressParser and ALIEXPRESS_SENDER_DOMAINS are importable from shipping_tracker.parsers
  </acceptance_criteria>
  <done>AliExpressParser matches AliExpress senders, extracts the tracking number via label then shape fallback, returns None for pre-shipment bodies, leaves carrier None, logs no PII, passes mypy --strict, and turns all PARSE-02 unit tests green. The parser is a single self-contained file owning its sender constant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Email body (untrusted) → extract() regex input | Adversarial/malformed body is fed directly to the regex engine |
| Parser output → downstream (Phase 4/5) | tracking_number is opaque data passed onward; never interpolated into SQL/shell here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Denial of Service (ReDoS) | _LABEL_RE / _SHAPE_RE in aliexpress.py | mitigate | Patterns use only bounded quantifiers ({8,35}, {10,16}, fixed letter prefixes) with no nested unbounded quantifiers; RESEARCH.md Security Domain rates ReDoS risk LOW. Acceptance criterion confirms both regexes compiled at module level |
| T-03-05 | Spoofing | _SHAPE_RE fallback | mitigate | Every fallback alternative requires a mandatory letter component, so a purely-numeric order reference (15–17 digits) cannot be mis-extracted as a tracking number (D-02, RESEARCH.md Pitfall 2); verified by test_extract_returns_none_preshipment |
| T-03-06 | Information Disclosure | aliexpress.py logging | mitigate | extract() emits no log calls; parser never references body/sender/tracking_number in any logger call (LOG-02); verified by test_extract_does_not_log_pii + grep acceptance criterion |
| T-03-07 | Denial of Service | extract() on no-tracking body | mitigate | Returns None rather than raising (D-05) so a pre-shipment email cannot crash the run; verified by test_extract_returns_none_preshipment |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH.md Package Legitimacy Audit: stdlib only); no install task to gate |
</threat_model>

<verification>
- `python -m mypy shipping_tracker/parsers/aliexpress.py` — zero errors
- All 7 PARSE-02 unit tests green (can_parse x2, extract label/fallback/none/carrier, no-PII)
- `pytest tests/test_aliexpress_parser.py tests/test_smoke.py -x -q` — dispatch/registry tests remain red pending Plan 03; all parser-unit + smoke tests green
</verification>

<success_criteria>
- AliExpressParser implements can_parse + extract per D-01/D-02/D-04/D-05
- Label-anchored primary path and shape-pattern fallback both work; pre-shipment returns None
- Numeric order references are never extracted (mandatory-letter shape constraint)
- mypy --strict clean; no PII logged; regexes ReDoS-safe and module-level
- Parser is one self-contained file owning ALIEXPRESS_SENDER_DOMAINS (D-01 drop-in property)
</success_criteria>

<output>
Create `.planning/phases/03-parser-layer/03-02-SUMMARY.md` when done
</output>
