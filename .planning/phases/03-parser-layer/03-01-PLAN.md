---
phase: 03-parser-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shipping_tracker/parsers/base.py
  - tests/fixtures/fake_aliexpress_email.py
  - tests/test_aliexpress_parser.py
  - tests/test_smoke.py
autonomous: true
requirements: [PARSE-01]
must_haves:
  truths:
    - "TrackingInfo can be constructed with carrier omitted; carrier defaults to None (D-04)"
    - "BaseParser.extract() return contract is TrackingInfo | None — no ValueError on no-tracking (D-05)"
    - "Synthetic FAKE-prefixed AliExpress body fixtures exist covering label, fallback, and pre-shipment cases"
    - "All 13 parser test functions exist and FAIL only because AliExpressParser / dispatch are not yet implemented"
  artifacts:
    - path: "shipping_tracker/parsers/base.py"
      provides: "TrackingInfo with carrier: str | None = None; extract() -> TrackingInfo | None"
      contains: "carrier: str | None = None"
    - path: "tests/fixtures/fake_aliexpress_email.py"
      provides: "Synthetic AliExpress email body + sender fixtures (FAKE-prefixed)"
      contains: "FAKE_ALIEXPRESS_SHIPPED_BODY"
    - path: "tests/test_aliexpress_parser.py"
      provides: "13 failing test stubs per 03-VALIDATION.md verification map"
      contains: "test_extract_label_anchored"
    - path: "tests/test_smoke.py"
      provides: "carrier=None default assertion (test_tracking_info_carrier_optional)"
      contains: "test_tracking_info_carrier_optional"
  key_links:
    - from: "tests/test_aliexpress_parser.py"
      to: "tests/fixtures/fake_aliexpress_email.py"
      via: "from tests.fixtures.fake_aliexpress_email import ..."
      pattern: "from tests.fixtures.fake_aliexpress_email import"
    - from: "tests/test_smoke.py"
      to: "shipping_tracker/parsers/base.py"
      via: "TrackingInfo(tracking_number=...) with carrier omitted"
      pattern: "TrackingInfo\\(tracking_number="
---

<objective>
Establish the Phase 3 contract and the failing-test scaffold for the parser vertical slice:
edit `TrackingInfo` / `BaseParser.extract()` per D-04 and D-05, create the synthetic
AliExpress fixtures, and write all 13 parser test functions (red) plus the smoke-test
`carrier=None` assertion. This is Wave 0 of the phase — it gives every later task automated
feedback (Nyquist Dimension 8) and defines the interfaces the parser and dispatch loop
implement against.

Purpose: Lock the output shape (`TrackingInfo | None`, optional carrier) and the test
expectations BEFORE any implementation, so the AliExpressParser (Plan 02) and dispatch loop
(Plan 03) are built against a fixed, tested contract — no scavenger hunt.
Output: Modified `base.py`, new `fake_aliexpress_email.py`, new `test_aliexpress_parser.py`
(failing), updated `test_smoke.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-parser-layer/03-CONTEXT.md
@.planning/phases/03-parser-layer/03-RESEARCH.md
@.planning/phases/03-parser-layer/03-PATTERNS.md
@.planning/phases/03-parser-layer/03-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit TrackingInfo and BaseParser.extract() contract (D-04, D-05)</name>
  <files>shipping_tracker/parsers/base.py, tests/test_smoke.py</files>
  <read_first>
    - shipping_tracker/parsers/base.py (the file being modified — current TrackingInfo dataclass and BaseParser.extract docstring/signature)
    - tests/test_smoke.py (existing test_tracking_info_dataclass at lines 33–37; the new assertion is added after it)
    - .planning/phases/03-parser-layer/03-PATTERNS.md §`shipping_tracker/parsers/base.py` (exact before/after for D-04 + D-05 edits) and §`tests/test_smoke.py` (the carrier-optional test)
  </read_first>
  <action>
    Per D-04: add `from __future__ import annotations` as the first import line of base.py
    (matches client.py convention). Change `TrackingInfo.carrier` from required `carrier: str`
    to `carrier: str | None = None`, keeping `tracking_number: str` first so the no-default
    field precedes the defaulted one (mypy --strict ordering — see RESEARCH.md Pitfall 3).
    Per D-05: change the abstract `extract` signature from `-> TrackingInfo` to
    `-> TrackingInfo | None`; revise its docstring to remove the `Raises: ValueError` clause
    and add a Returns note that it returns `None` when the email matches but contains no
    tracking number (routine pre-shipment "order confirmed" emails). Do not change `can_parse`.
    In tests/test_smoke.py, leave `test_tracking_info_dataclass` unchanged (positional
    construction still works) and add `test_tracking_info_carrier_optional` immediately after it
    asserting `TrackingInfo(tracking_number="FAKENUMBER").carrier is None`. No fenced code in
    this action — replicate the exact strings shown in 03-PATTERNS.md.
  </action>
  <verify>
    <automated>python -m mypy shipping_tracker/parsers/base.py</automated>
    <automated>pytest tests/test_smoke.py::test_tracking_info_carrier_optional tests/test_smoke.py::test_tracking_info_dataclass tests/test_smoke.py::test_base_parser_is_abstract -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' shipping_tracker/parsers/base.py | grep -c "carrier: str | None = None"` returns 1
    - base.py `extract` signature is `-> TrackingInfo | None` and its docstring contains no "Raises" / "ValueError"
    - base.py first import line is `from __future__ import annotations`
    - `python -m mypy shipping_tracker/parsers/base.py` reports zero errors
    - `pytest tests/test_smoke.py::test_tracking_info_carrier_optional -x` passes
    - `pytest tests/test_smoke.py::test_tracking_info_dataclass -x` still passes (positional construction unbroken — RESEARCH.md Pitfall 4)
  </acceptance_criteria>
  <done>TrackingInfo.carrier is optional with None default, extract() returns TrackingInfo | None with a docstring that no longer promises ValueError, smoke tests for both the existing and the new carrier-default assertion pass, and mypy --strict is clean on base.py.</done>
</task>

<task type="auto">
  <name>Task 2: Create synthetic AliExpress fixtures and the 13 failing parser tests</name>
  <files>tests/fixtures/fake_aliexpress_email.py, tests/test_aliexpress_parser.py</files>
  <read_first>
    - tests/fixtures/fake_gmail_message.py (analog — privacy docstring format + FAKE-prefix convention + .example.com domains)
    - tests/conftest.py (synthetic-fixture pattern, PRIVACY docstring rules)
    - tests/test_smoke.py (analog test style — plain functions, `-> None`, single-invariant docstring)
    - shipping_tracker/gmail/client.py (RawEmail dataclass shape: message_id, sender, body — used by dispatch integration tests)
    - .planning/phases/03-parser-layer/03-PATTERNS.md §`tests/fixtures/fake_aliexpress_email.py` (required fixture constants + values) and §`tests/test_aliexpress_parser.py` (exact test bodies for can_parse/extract/dispatch)
    - .planning/phases/03-parser-layer/03-VALIDATION.md §Per-Task Verification Map (the canonical list of 13 test function names + commands)
  </read_first>
  <action>
    Create tests/fixtures/fake_aliexpress_email.py with the PRIVACY module docstring and the
    FAKE-prefixed constants from 03-PATTERNS.md: FAKE_ALIEXPRESS_SHIPPED_BODY (label "Tracking
    number:" + a courier-named "Carrier:" line, tracking FAKELP00FAKE00001),
    FAKE_ALIEXPRESS_LOGISTICS_BODY ("Logistics No." variant), FAKE_ALIEXPRESS_TRACKING_NO_BODY
    ("Tracking No." variant), FAKE_ALIEXPRESS_PRESHIPMENT_BODY (no tracking number, numeric
    order reference only — D-05 case), FAKE_ALIEXPRESS_NOLABEL_BODY (shape-fallback case,
    FAKEYT00000FAKE0001), FAKE_ALIEXPRESS_SENDER and FAKE_OTHER_SENDER (both `.example.com`,
    non-matching). All values synthetic, no real tracking numbers / addresses / order refs.
    Create tests/test_aliexpress_parser.py with exactly the 13 test functions named in
    03-VALIDATION.md: test_tracking_info_carrier_optional, test_can_parse_known_domains,
    test_can_parse_rejects_other_senders, test_extract_label_anchored,
    test_extract_shape_fallback, test_extract_returns_none_preshipment, test_extract_carrier_none,
    test_extract_does_not_log_pii, test_dispatch_matched_email, test_dispatch_no_match_skips,
    test_dispatch_preshipment_skips, test_registry_drop_in (and the base-ABC smoke parity is
    covered in test_smoke). For can_parse tests, construct matching senders inline using the
    domain constants (e.g. `f"shipping{domain}"`) — never embed `@mail.aliexpress.com` as a
    fixture value. Dispatch tests import `PARSERS` from shipping_tracker.main and build RawEmail
    objects directly. These tests MUST currently FAIL (collection/import errors are acceptable
    red, since aliexpress.py and main.PARSERS do not yet exist) — do NOT implement the parser or
    dispatch loop in this plan. No fenced code in this action — copy the exact test bodies from
    03-PATTERNS.md.
  </action>
  <verify>
    <automated>python -c "import tests.fixtures.fake_aliexpress_email as f; assert f.FAKE_ALIEXPRESS_SHIPPED_BODY and f.FAKE_OTHER_SENDER"</automated>
    <automated>pytest tests/test_aliexpress_parser.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/fake_aliexpress_email.py imports cleanly and exposes all 7 FAKE_* constants named in 03-PATTERNS.md
    - The fixture module docstring contains the word PRIVACY and no value contains a real aliexpress.com domain
    - `grep -c "FAKE" tests/fixtures/fake_aliexpress_email.py` is non-zero and no non-FAKE tracking-looking tokens are present
    - `pytest tests/test_aliexpress_parser.py --collect-only -q` lists all 12 test functions defined in this file (per 03-VALIDATION.md)
    - Running `pytest tests/test_aliexpress_parser.py -q` shows the suite RED (failures or collection errors due to missing AliExpressParser / main.PARSERS) — this is the expected Wave 0 state
  </acceptance_criteria>
  <done>The synthetic fixtures and all parser test functions named in 03-VALIDATION.md exist, collect, and fail only because the implementation (Plan 02) and dispatch wiring (Plan 03) are not yet present. No real data appears anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gmail email body → parser/test input | Untrusted email body text is the raw input; fixtures stand in for it here |
| Source/test files → public git history | Repo ships public; any PII committed is permanent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | tests/fixtures/fake_aliexpress_email.py | mitigate | All fixture values FAKE-prefixed / `.example.com`; PRIVACY docstring required; acceptance criterion greps for absence of real aliexpress.com domains (LOG-02 / CLAUDE.md privacy) |
| T-03-02 | Information Disclosure | tests/test_aliexpress_parser.py | mitigate | test_extract_does_not_log_pii asserts no body / tracking number text appears in any log record (LOG-02) |
| T-03-03 | Denial of Service | base.py extract() contract | mitigate | Contract returns `TrackingInfo | None` (no exception flow on pre-shipment emails per D-05) so a no-tracking email can never crash the run — enforced by tests in Plan 02/03 |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in Phase 3 (RESEARCH.md Package Legitimacy Audit: stdlib only — section intentionally empty); no install task to gate |
</threat_model>

<verification>
- `python -m mypy shipping_tracker/parsers/base.py` — zero errors
- `pytest tests/test_smoke.py -q` — all green (existing + new carrier-optional test)
- `pytest tests/test_aliexpress_parser.py --collect-only -q` — collects all 12 functions
- `pytest tests/test_aliexpress_parser.py -q` — RED (expected Wave 0 state; goes green after Plans 02 + 03)
</verification>

<success_criteria>
- TrackingInfo.carrier is `str | None = None`; extract() returns `TrackingInfo | None` (D-04, D-05)
- Smoke suite green including the new carrier-default assertion
- All synthetic fixtures + 12 parser test functions exist, collect, and are red pending implementation
- No real tracking numbers, email addresses, or order references in any new file (privacy non-negotiable)
</success_criteria>

<output>
Create `.planning/phases/03-parser-layer/03-01-SUMMARY.md` when done
</output>
