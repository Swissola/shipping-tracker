---
phase: 04-deduplication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/fake_db.py
  - tests/conftest.py
  - tests/test_db.py
autonomous: true
requirements: [DEDUP-01, DEDUP-02, DEDUP-03, DEDUP-04, DEDUP-05]
nyquist_compliant: true

must_haves:
  truths:
    - "All 15 DEDUP test functions exist in tests/test_db.py and fail RED at import (db.py / registrar.py not yet written)"
    - "An in-memory sqlite3 db_conn fixture is available to every test"
    - "All test data is FAKE-prefixed synthetic — no real tracking numbers or message ids"
    - "The retry-proof test (success criterion 4) is present and asserts fail->no-row->retry-succeeds"
  artifacts:
    - path: "tests/fixtures/fake_db.py"
      provides: "FAKE-prefixed message-id and tracking-number constants + privacy docstring"
      contains: "FAKE_TRACKING_NUMBER_1"
    - path: "tests/conftest.py"
      provides: "db_conn in-memory sqlite3 fixture with init_db pre-called"
      contains: "def db_conn"
    - path: "tests/test_db.py"
      provides: "15 DEDUP-01..05 + D-03 + D-09 test functions"
      contains: "def test_retry_proof"
  key_links:
    - from: "tests/test_db.py"
      to: "shipping_tracker.db"
      via: "import init_db, is_email_processed, is_tracking_registered, register_and_persist"
      pattern: "from shipping_tracker.db import"
    - from: "tests/conftest.py"
      to: "shipping_tracker.db.init_db"
      via: "fixture calls init_db(conn) on an in-memory connection"
      pattern: "init_db\\(conn\\)"
---

<objective>
Wave 0 (Nyquist) test scaffold for Phase 4 deduplication. Author the 15 test
functions from 04-VALIDATION.md, the FAKE-prefixed fixture data, and the shared
in-memory `db_conn` connection fixture — BEFORE any source module exists. These
tests intentionally fail RED (import errors for `shipping_tracker.db` /
`shipping_tracker.registrar`), pinning the exact contract that Plan 02 (source)
and Plan 03 (wiring) must satisfy. Covers DEDUP-01..05, D-03, D-09 per the
Validation map.

Purpose: Nyquist sampling requires the tests to precede the source so feedback
latency stays ~5s and the four ROADMAP success criteria (esp. criterion 4, the
retry proof) are provable, not asserted by hand.
Output: tests/fixtures/fake_db.py, tests/conftest.py (extended), tests/test_db.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deduplication/04-CONTEXT.md
@.planning/phases/04-deduplication/04-RESEARCH.md
@.planning/phases/04-deduplication/04-PATTERNS.md
@.planning/phases/04-deduplication/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FAKE fixture data + db_conn connection fixture</name>
  <files>tests/fixtures/fake_db.py, tests/conftest.py</files>
  <read_first>
    - tests/fixtures/fake_aliexpress_email.py — the privacy-docstring + FAKE-prefix constant analog (04-PATTERNS.md §tests/fixtures/fake_db.py)
    - tests/conftest.py — existing @pytest.fixture style to extend (04-PATTERNS.md §tests/conftest.py)
    - .planning/phases/04-deduplication/04-PATTERNS.md §tests/fixtures/fake_db.py and §tests/conftest.py (exact constant names + fixture body)
    - ./CLAUDE.md §Constraints — synthetic-only fixtures, no PII
  </read_first>
  <action>
    Create tests/fixtures/fake_db.py as a pure module-level-constants file (no
    functions, no classes) with the privacy docstring from 04-PATTERNS.md.
    Declare exactly: FAKE_MESSAGE_ID_1 = "FAKEMSGID001", FAKE_MESSAGE_ID_2 =
    "FAKEMSGID002", FAKE_MESSAGE_ID_DUP = "FAKEMSGID003",
    FAKE_TRACKING_NUMBER_1 = "FAKETRACK001CN", FAKE_TRACKING_NUMBER_2 =
    "FAKETRACK002CN". All values FAKE-prefixed, no real carrier format (per
    D-12 message_id is the opaque Gmail id — non-PII — but stays synthetic in
    tests).
    Extend tests/conftest.py: add `import sqlite3` and
    `from shipping_tracker.db import init_db` at the top, and a generator-form
    `db_conn` fixture that does `conn = sqlite3.connect(":memory:")`,
    `init_db(conn)`, `yield conn`, `conn.close()`. Keep the existing
    synthetic_email_body fixture untouched. The new import of
    shipping_tracker.db will not resolve yet — that is the intended RED state
    until Plan 02 ships db.py.
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('tests/fixtures/fake_db.py').read()); ast.parse(open('tests/conftest.py').read()); print('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/fake_db.py contains FAKE_TRACKING_NUMBER_1, FAKE_MESSAGE_ID_1, and FAKE_MESSAGE_ID_DUP as module-level string constants
    - tests/fixtures/fake_db.py and tests/conftest.py both parse as valid Python (verify command prints parse-ok)
    - tests/conftest.py defines `def db_conn(` and calls `init_db(conn)` on a `:memory:` connection
    - No value in either file is a real tracking number, email address, or order reference (all FAKE-prefixed)
  </acceptance_criteria>
  <done>fake_db.py constants and the db_conn fixture exist; both files parse cleanly; data is synthetic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The 15 DEDUP test functions (RED)</name>
  <files>tests/test_db.py</files>
  <behavior>
    - DEDUP-01: test_init_db_creates_processed_emails — table exists after init_db
    - DEDUP-01: test_init_db_idempotent — calling init_db twice raises nothing
    - DEDUP-02: test_init_db_creates_registered_tracking — table exists with nullable last_status/last_status_at
    - DEDUP-02: test_user_version — PRAGMA user_version returns 1 after init
    - DEDUP-03: test_is_email_processed_known — True after a processed_emails row is inserted
    - DEDUP-03: test_is_email_processed_unknown — False for an absent message_id
    - DEDUP-03: test_dispatch_skips_processed_email — already-seen email is skipped (integration: drive register_and_persist / is_email_processed over db_conn)
    - DEDUP-04: test_is_tracking_registered — True/False per presence in registered_tracking
    - DEDUP-04: test_dispatch_skips_registered_tracking — registrar NOT called when tracking already registered (assert via a spy registrar that flips a flag)
    - D-03: test_dup_notification_marks_email_processed — DEDUP-04 branch writes message_id via INSERT OR IGNORE, registrar not called
    - DEDUP-05: test_register_and_persist_success — both rows present after success registrar
    - DEDUP-05: test_register_and_persist_fail_returns_false — neither row present after False registrar; returns False
    - DEDUP-05: test_register_and_persist_raises_rolls_back — raising registrar leaves neither row (rollback) and propagates
    - DEDUP-05: test_retry_proof — fail run -> zero rows -> success run -> both rows present (success criterion 4)
    - D-09: test_null_registrar_defers — NullRegistrar returns False, logs at debug, no tracking_number in any log record
  </behavior>
  <read_first>
    - .planning/phases/04-deduplication/04-VALIDATION.md §Per-Task Verification Map (exact test names) and §Retry Proof Test Design
    - .planning/phases/04-deduplication/04-RESEARCH.md §Retry Proof Test Design (full test_retry_proof outline) + §Code Examples (function signatures the tests call)
    - .planning/phases/04-deduplication/04-PATTERNS.md §tests/test_db.py (import block, fake-registrar lambdas, caplog PII-safety assertion, retry-proof body)
    - tests/test_aliexpress_parser.py — the unit+integration mix + caplog PII assertion analog
    - tests/fixtures/fake_db.py — the FAKE constants created in Task 1
  </read_first>
  <action>
    Create tests/test_db.py with the module privacy docstring and the import
    block from 04-PATTERNS.md: import sqlite3, pytest, the four names from
    shipping_tracker.db (init_db, is_email_processed, is_tracking_registered,
    register_and_persist), NullRegistrar and Registrar from
    shipping_tracker.registrar, and the FAKE_* constants from
    tests.fixtures.fake_db. Write all 15 functions named EXACTLY as in the
    behavior list (they must match the 04-VALIDATION.md commands). Use the
    db_conn fixture for table/predicate tests. Use inline fake registrars:
    `lambda tn, c: True` (success), `lambda tn, c: False` (fail), and a
    `def raising_registrar(tracking_number, carrier): raise RuntimeError("synthetic API failure")`
    — synthetic message only, never embedding a tracking number. For
    test_dispatch_skips_registered_tracking and
    test_dup_notification_marks_email_processed, use a spy registrar (a small
    callable recording whether it was invoked) and assert it was NOT called.
    test_retry_proof must build its own `sqlite3.connect(":memory:")` per the
    RESEARCH outline (two sequential register_and_persist calls — fail then
    success — asserting row absence then presence). All SQL in tests uses
    parameterized `?` placeholders, never f-strings (ASVS V5). All test data is
    FAKE-prefixed. The file fails RED at import because db.py / registrar.py do
    not exist yet — that is the intended Nyquist state for Wave 0.
  </action>
  <verify>
    <automated>python -m pytest tests/test_db.py -q --collect-only 2>&1 | grep -Eiq "error|no module named shipping_tracker.db|cannot import" && echo "RED-as-expected (collection blocked by missing db.py)" || python -c "import ast; ast.parse(open('tests/test_db.py').read()); print('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_db.py defines all 15 functions named exactly per 04-VALIDATION.md (incl. `def test_retry_proof` and `def test_null_registrar_defers`)
    - tests/test_db.py parses as valid Python (no syntax errors)
    - Collection currently fails RED with an import error for shipping_tracker.db / shipping_tracker.registrar (expected until Plan 02) — verify command confirms RED-as-expected
    - No SQL in the file uses f-strings or string concatenation for values; all use `?` placeholders
    - test_null_registrar_defers asserts no FAKE_TRACKING_NUMBER appears in any caplog record
  </acceptance_criteria>
  <done>All 15 named tests exist, parse cleanly, and fail RED only because the Wave-1 source modules are not yet present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures -> repo / git history | Synthetic test data is committed; must contain no real PII |
| test SQL -> in-memory DB | Test queries exercise the same parameterized-query discipline as production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information disclosure | tests/fixtures/fake_db.py, tests/test_db.py committed to public repo | mitigate | All values FAKE-prefixed synthetic; privacy docstring; no real tracking numbers/message ids/emails (CLAUDE.md, verified by acceptance criteria) |
| T-04-02 | Information disclosure | test_null_registrar_defers caplog assertion | mitigate | Test itself asserts no tracking_number leaks into log records (LOG-02 regression guard) |
| T-04-03 | Tampering | SQL in tests | accept (low) | Tests use parameterized `?` placeholders by convention; in-memory only, no untrusted input — low risk |
| T-04-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase — all stdlib + existing pytest (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `python -m pytest tests/test_db.py -q --collect-only` fails RED with an import error for the not-yet-written source modules (expected Wave 0 state).
- Both new files and the extended conftest.py parse as valid Python.
- `ruff check tests/` reports no new violations on the authored files.
</verification>

<success_criteria>
- 15 test functions authored with the exact names the 04-VALIDATION.md commands target.
- FAKE-prefixed fixture constants and the reusable db_conn fixture in place.
- Tests fail RED solely due to missing shipping_tracker.db / shipping_tracker.registrar — the Nyquist contract for Plan 02 to satisfy.
- No PII in any authored test artifact.
</success_criteria>

<output>
Create `.planning/phases/04-deduplication/04-01-SUMMARY.md` when done.
</output>
