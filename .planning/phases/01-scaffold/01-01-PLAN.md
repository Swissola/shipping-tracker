---
phase: 01-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - .env.example
  - shipping_tracker/__init__.py
  - shipping_tracker/__main__.py
  - shipping_tracker/main.py
  - shipping_tracker/logging_config.py
  - shipping_tracker/parsers/__init__.py
  - shipping_tracker/parsers/base.py
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-07

must_haves:
  truths:
    - "python -m shipping_tracker exits 0 from the repo root"
    - "ruff check . and ruff format --check . pass with zero violations"
    - "mypy shipping_tracker/ --strict passes with zero type errors"
    - ".env is excluded from git (git check-ignore .env outputs .env)"
    - "shipping_tracker is importable as a Python package"
    - "BaseParser ABC and TrackingInfo dataclass exist and are fully typed"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package manifest with all tool config (ruff, mypy, pytest, setuptools)"
      contains: "[tool.ruff]"
    - path: ".gitignore"
      provides: "Privacy enforcement — .env, *.db, token.json, logs/ excluded from git"
      contains: ".env"
    - path: ".env.example"
      provides: "Placeholder env vars committed to git"
      contains: "SEVENTEEN_TRACK_API_KEY"
    - path: "shipping_tracker/__main__.py"
      provides: "python -m entry point"
      contains: "sys.exit(main())"
    - path: "shipping_tracker/main.py"
      provides: "Pipeline orchestrator stub — main() returns int"
      exports: ["main"]
    - path: "shipping_tracker/logging_config.py"
      provides: "configure_logging() — structlog + RotatingFileHandler, no stdout"
      exports: ["configure_logging"]
    - path: "shipping_tracker/parsers/base.py"
      provides: "BaseParser ABC + TrackingInfo dataclass"
      exports: ["BaseParser", "TrackingInfo"]
  key_links:
    - from: "shipping_tracker/__main__.py"
      to: "shipping_tracker/main.py"
      via: "from shipping_tracker.main import main"
      pattern: "from shipping_tracker\\.main import main"
    - from: "shipping_tracker/main.py"
      to: "shipping_tracker/logging_config.py"
      via: "from shipping_tracker.logging_config import configure_logging"
      pattern: "configure_logging\\(\\)"
---

<objective>
Create the complete Python package skeleton: pyproject.toml manifest, .gitignore,
.env.example, the shipping_tracker package with entry point, pipeline stub,
logging config, and BaseParser ABC.

Purpose: This is the Walking Skeleton — after this plan executes, `python -m
shipping_tracker` runs to completion, the toolchain (ruff, mypy) is green,
and the pluggable parser architecture is in place. Every subsequent phase adds
to this structure without renegotiating it.

Output: 9 files that form the complete package foundation. D-01 through D-09
are all implemented in this plan.
</objective>

<execution_context>
@C:/Users/marti/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/marti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-scaffold/01-CONTEXT.md
@.planning/phases/01-scaffold/01-RESEARCH.md
@.planning/phases/01-scaffold/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Project manifest, .gitignore, and .env.example (SETUP-01, SETUP-02, SETUP-03, SETUP-07)</name>
  <files>pyproject.toml, .gitignore, .env.example</files>
  <read_first>
    - .planning/phases/01-scaffold/01-PATTERNS.md §pyproject.toml — full verified toml structure to use as the authoritative reference for all sections, versions, and tool configs
    - .planning/phases/01-scaffold/01-PATTERNS.md §.gitignore — privacy-critical entries (non-negotiable per CLAUDE.md)
    - .planning/phases/01-scaffold/01-PATTERNS.md §.env.example — placeholder structure
    - .planning/phases/01-scaffold/01-CONTEXT.md §Implementation Decisions — D-01 through D-10 (all locked decisions)
  </read_first>
  <action>
    Create pyproject.toml at repo root using the exact structure from PATTERNS.md §pyproject.toml.
    Key requirements:
    - [build-system]: setuptools>=77, build-backend = "setuptools.build_meta"
    - [project]: name = "shipping-tracker", version = "0.1.0", requires-python = ">=3.11"
    - [project.dependencies]: httpx>=0.28, structlog>=25.5, python-dotenv>=1.2 (runtime only, per D-09)
    - [project.optional-dependencies] dev group: ruff>=0.15, mypy>=2.1, pytest>=9.0, pre-commit>=4.6 (per D-09)
    - [project.scripts]: shipping-tracker = "shipping_tracker.main:main" (NOT __main__:main — per PATTERNS.md Pitfall 5 note)
    - [tool.ruff]: target-version = "py311", line-length = 88; [tool.ruff.lint] select = ["E","F","I","W","UP"]; [tool.ruff.format] quote-style = "double", indent-style = "space" (per D-02 / SETUP-02)
    - [tool.mypy]: strict = true, python_version = "3.11"; [[tool.mypy.overrides]] module = ["google.*","googleapiclient.*"], ignore_missing_imports = true (per D-08 / SETUP-03 — overrides block anticipates Phase 2)
    - [tool.pytest.ini_options]: testpaths = ["tests"], pythonpath = ["."] (per SETUP-04)
    - [tool.setuptools.packages.find]: where = ["."], include = ["shipping_tracker*"]

    Create .gitignore at repo root with all privacy-critical entries from PATTERNS.md §.gitignore:
    - .env, token.json, credentials.json (SETUP-07 — NON-NEGOTIABLE, project will be public)
    - *.db, *.sqlite3
    - logs/ (D-06 path is in .gitignore since logs/ is runtime-created)
    - __pycache__/, *.py[cod], *.egg-info/, dist/, .venv/, venv/

    Create .env.example at repo root using PATTERNS.md §.env.example structure:
    - SEVENTEEN_TRACK_API_KEY=your_api_key_here
    - LOG_LEVEL=WARNING
    - LOG_PATH=logs/shipping-tracker.log
    - Include comment block explaining the .env → .env.example relationship (per SETUP-07)
  </action>
  <verify>
    <automated>python -c "import tomllib; tomllib.loads(open('pyproject.toml').read()); print('TOML valid')" && python -c "open('.env.example').read(); print('.env.example exists')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml parses as valid TOML without errors
    - pyproject.toml contains [tool.ruff] section
    - pyproject.toml contains [tool.mypy] with strict = true
    - pyproject.toml contains [tool.pytest.ini_options] with testpaths = ["tests"]
    - pyproject.toml [project.scripts] entry is shipping-tracker = "shipping_tracker.main:main"
    - pyproject.toml [project.optional-dependencies] dev group exists
    - .gitignore contains .env on its own line
    - .gitignore contains token.json, *.db, logs/
    - .env.example contains SEVENTEEN_TRACK_API_KEY=your_api_key_here
    - .env does NOT exist (or if it does, git check-ignore .env outputs .env — confirming it is excluded from tracking)
  </acceptance_criteria>
  <done>pyproject.toml is a valid manifest with all tool configuration. .gitignore excludes all secrets. .env.example is committed with placeholders only.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Python package files — entry point, pipeline stub, logging config, BaseParser (SETUP-01, SETUP-02, SETUP-03)</name>
  <files>
    shipping_tracker/__init__.py,
    shipping_tracker/__main__.py,
    shipping_tracker/main.py,
    shipping_tracker/logging_config.py,
    shipping_tracker/parsers/__init__.py,
    shipping_tracker/parsers/base.py
  </files>
  <read_first>
    - .planning/phases/01-scaffold/01-PATTERNS.md §shipping_tracker/__main__.py — exact three-line pattern (non-negotiable minimal runner)
    - .planning/phases/01-scaffold/01-PATTERNS.md §shipping_tracker/main.py — fully typed stub pattern with load_dotenv() first, configure_logging() second
    - .planning/phases/01-scaffold/01-PATTERNS.md §shipping_tracker/logging_config.py — full configure_logging() implementation with RotatingFileHandler, NO StreamHandler
    - .planning/phases/01-scaffold/01-PATTERNS.md §shipping_tracker/parsers/base.py — BaseParser ABC + TrackingInfo dataclass (architecture constraint)
    - .planning/phases/01-scaffold/01-RESEARCH.md §Anti-Patterns — patterns to avoid in every file
    - .planning/phases/01-scaffold/01-CONTEXT.md §Implementation Decisions — D-01, D-03, D-05, D-06, D-07, D-08
  </read_first>
  <action>
    Create shipping_tracker/__init__.py: single docstring line only — "shipping-tracker: Gmail → 17track automation." No imports (circular import risk as package grows).

    Create shipping_tracker/__main__.py using the exact three-line pattern from PATTERNS.md:
    - Module docstring: 'Entry point for `python -m shipping_tracker`.'
    - import sys
    - from shipping_tracker.main import main
    - sys.exit(main())
    No function definitions, no logic. Module-level sys.exit() is intentional (per D-01, D-02).

    Create shipping_tracker/main.py as the pipeline orchestrator stub:
    - Function signature: def main() -> int:
    - Body: call load_dotenv() FIRST (before any env reads or library imports that might call basicConfig), then configure_logging(), then logger.warning("shipping_tracker started — pipeline stub, no work performed"), then return 0
    - Module-level: import logging; from dotenv import load_dotenv; from shipping_tracker.logging_config import configure_logging; logger = logging.getLogger(__name__)
    - Full docstring on main() explaining the return convention and Phase 1 stub status (per D-03)

    Create shipping_tracker/logging_config.py using the full pattern from PATTERNS.md §logging_config.py:
    - Function signature: def configure_logging(log_path: str = "logs/shipping-tracker.log", log_level: int = logging.WARNING) -> None:
    - First line of body: os.makedirs(os.path.dirname(log_path) or "logs", exist_ok=True) — mandatory, logs/ is in .gitignore (Pitfall 4)
    - shared_processors list with: add_log_level, add_logger_name, TimeStamper(fmt="iso"), StackInfoRenderer(), format_exc_info
    - ProcessorFormatter with [remove_processors_meta, JSONRenderer()] — compact JSON, no whitespace
    - RotatingFileHandler: maxBytes=10*1024*1024 (10 MB per D-06), backupCount=3 (per D-06)
    - root_logger.addHandler(handler); root_logger.setLevel(log_level) — NO StreamHandler (cron silence per D-07, LOG-03)
    - structlog.configure() with ProcessorFormatter.wrap_for_formatter, LoggerFactory(), BoundLogger, cache_logger_on_first_use=True

    Create shipping_tracker/parsers/__init__.py: single docstring — "Parser sub-package — pluggable email parser implementations."

    Create shipping_tracker/parsers/base.py using the full pattern from PATTERNS.md §parsers/base.py:
    - from abc import ABC, abstractmethod
    - from dataclasses import dataclass
    - @dataclass class TrackingInfo with fields: tracking_number: str, carrier: str — typed dataclass required for mypy strict
    - class BaseParser(ABC) with @abstractmethod def can_parse(self, email_body: str, sender: str) -> bool and @abstractmethod def extract(self, email_body: str) -> TrackingInfo
    - Both methods have complete docstrings with Args and Returns sections (mypy strict requires no bare ellipsis without annotations)

    Privacy check: none of these files may contain real email addresses, tracking numbers, order IDs, or personal names.
  </action>
  <verify>
    <automated>pip install -e ".[dev]" --quiet && python -c "import shipping_tracker; print('importable')" && python -m shipping_tracker && ruff check . && ruff format --check . && mypy shipping_tracker/ --strict</automated>
  </verify>
  <acceptance_criteria>
    - python -c "import shipping_tracker" exits 0
    - python -m shipping_tracker exits 0 (no error output)
    - shipping_tracker/__main__.py contains exactly "sys.exit(main())" at module level and no function definitions
    - shipping_tracker/main.py contains "def main() -> int:" with return type annotation
    - shipping_tracker/logging_config.py contains "RotatingFileHandler" and does NOT contain "StreamHandler" as an addHandler call
    - shipping_tracker/parsers/base.py contains "class BaseParser(ABC):" and "@dataclass" and "class TrackingInfo:"
    - ruff check . exits 0 with zero violations
    - ruff format --check . exits 0 with zero violations
    - mypy shipping_tracker/ --strict exits 0 with zero errors
    - No real email addresses, tracking numbers, or personal data appear in any of the 6 files
  </acceptance_criteria>
  <done>Package is installable and importable. python -m shipping_tracker runs and exits 0. ruff and mypy --strict pass. BaseParser and TrackingInfo stubs are in place per the architecture constraint in CLAUDE.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → git | .env and credential files must never cross into git history |
| .env → os.environ | Secrets loaded at runtime only; never appear in source code |
| log file → filesystem | Log output must not contain PII (email addresses, tracking numbers) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .gitignore / .env | mitigate | .gitignore excludes .env, token.json, credentials.json, *.db, logs/ before any secret is ever written; verified by `git check-ignore .env` |
| T-01-02 | Information Disclosure | logging_config.py / log output | mitigate | No StreamHandler added to root logger; RotatingFileHandler only; Phase 1 stub logs only a single warning message with no user data — PII-safe by construction |
| T-01-03 | Information Disclosure | test fixtures | mitigate | All test data synthetic (FAKE prefix tracking numbers, FAKECARRIER); enforced by CLAUDE.md privacy constraint and privacy docstring in conftest.py |
| T-01-04 | Information Disclosure | source/git history | mitigate | .env.example committed with placeholder values only; no real API keys, credentials, or personal data in any source file |
| T-01-SC | Tampering | pip install (httpx, structlog, python-dotenv, ruff, mypy, pytest, pre-commit) | accept | All packages verified via slopcheck 0.6.1 + PyPI on 2026-05-31 (see RESEARCH.md §Package Legitimacy Audit); all returned [OK], no [ASSUMED] or [SUS] packages |
</threat_model>

<verification>
After both tasks complete, run the full Phase 1 toolchain check from the repo root:

```
ruff check . && ruff format --check . && mypy shipping_tracker/ --strict && python -m shipping_tracker && git check-ignore .env
```

All five commands must exit 0. Expected outputs:
- ruff check: no output (zero violations)
- ruff format --check: no output (zero violations)
- mypy --strict: "Success: no issues found in N source files"
- python -m shipping_tracker: exits silently with code 0 (no stdout per cron silence requirement)
- git check-ignore .env: outputs ".env"
</verification>

<success_criteria>
1. python -m shipping_tracker exits 0 from the repo root — the Walking Skeleton runs
2. ruff check . and ruff format --check . pass — zero violations on initial codebase
3. mypy shipping_tracker/ --strict passes — zero type errors
4. shipping_tracker.parsers.base exports BaseParser and TrackingInfo — pluggable architecture in place
5. .env is excluded from git (git check-ignore .env confirms)
6. .env.example is committed with SEVENTEEN_TRACK_API_KEY placeholder only
7. No PII anywhere in the 9 files created by this plan
</success_criteria>

<output>
Create `.planning/phases/01-scaffold/01-01-SUMMARY.md` when done.
</output>
