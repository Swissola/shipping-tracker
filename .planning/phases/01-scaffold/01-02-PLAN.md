---
phase: 01-scaffold
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .pre-commit-config.yaml
  - .github/workflows/ci.yml
  - tests/__init__.py
  - tests/conftest.py
  - tests/fixtures/.gitkeep
  - tests/test_smoke.py
autonomous: true
requirements:
  - SETUP-04
  - SETUP-05
  - SETUP-06

must_haves:
  truths:
    - "pytest discovers and runs the test suite, zero failures"
    - "pre-commit run --all-files passes (ruff + mypy hooks both green)"
    - "GitHub Actions CI workflow file exists and triggers on push and pull_request"
    - "tests/fixtures/ directory exists and is tracked in git"
    - "Smoke test verifies python -m shipping_tracker exits 0"
    - "All test fixtures use synthetic data only — no real PII"
  artifacts:
    - path: ".pre-commit-config.yaml"
      provides: "ruff-check + ruff-format + mypy hooks"
      contains: "ruff-pre-commit"
    - path: ".github/workflows/ci.yml"
      provides: "CI matrix: lint (py3.11) + test (py3.11/3.12/3.13)"
      contains: "python-version"
    - path: "tests/conftest.py"
      provides: "Shared synthetic fixtures"
      contains: "FAKE1234567890"
    - path: "tests/test_smoke.py"
      provides: "Smoke tests for importability and entry point"
      exports: ["test_package_importable", "test_entry_point_exits_zero"]
  key_links:
    - from: ".pre-commit-config.yaml"
      to: "ruff-pre-commit"
      via: "rev: v0.15.15"
      pattern: "v0\\.15\\.15"
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml"
      via: "pip install -e '.[dev]'"
      pattern: "pip install -e '\\.\\[dev\\]'"
---

<objective>
Add toolchain enforcement and test infrastructure on top of the package skeleton
from Plan 01: pre-commit hooks for ruff + mypy, GitHub Actions CI matrix
(lint + test), and a pytest suite with smoke tests and the synthetic fixture
directory.

Purpose: After this plan, every git commit triggers ruff + mypy checks locally
(pre-commit), and every push triggers the full CI suite remotely (GitHub Actions).
The test infrastructure is ready for Phases 2–5 to add real tests.

Output: 6 files. The phase is complete — all 7 SETUP requirements satisfied.
</objective>

<execution_context>
@C:/Users/marti/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/marti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-scaffold/01-CONTEXT.md
@.planning/phases/01-scaffold/01-RESEARCH.md
@.planning/phases/01-scaffold/01-PATTERNS.md
@.planning/phases/01-scaffold/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-commit config and GitHub Actions CI workflow (SETUP-05, SETUP-06)</name>
  <files>.pre-commit-config.yaml, .github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-scaffold/01-PATTERNS.md §.pre-commit-config.yaml — exact verified YAML; hook order is mandatory (ruff-check before ruff-format); additional_dependencies for mypy hook is mandatory
    - .planning/phases/01-scaffold/01-PATTERNS.md §.github/workflows/ci.yml — exact verified YAML; lint job (py3.11 only) + test matrix (py3.11/3.12/3.13) per D-10
    - .planning/phases/01-scaffold/01-CONTEXT.md §D-10 — CI requirements: ubuntu-latest, matrix 3.11/3.12/3.13, triggers push and pull_request
    - .planning/phases/01-scaffold/01-RESEARCH.md §Pitfall 2 — mypy additional_dependencies is mandatory; without it pre-commit cannot resolve imports
  </read_first>
  <action>
    Create .pre-commit-config.yaml using the exact pattern from PATTERNS.md §.pre-commit-config.yaml:
    - First hook repo: https://github.com/astral-sh/ruff-pre-commit, rev: v0.15.15
      - Hook id: ruff-check, args: [--fix] — MUST come before ruff-format (PATTERNS.md verified note)
      - Hook id: ruff-format (no args)
    - Second hook repo: https://github.com/pre-commit/mirrors-mypy, rev: v2.1.0
      - Hook id: mypy, args: [--strict]
      - additional_dependencies: [httpx, structlog, python-dotenv] — mandatory per Pitfall 2; without these, mypy in the isolated pre-commit virtualenv cannot resolve imports and silently fails

    Create .github/workflows/ci.yml using the exact pattern from PATTERNS.md §.github/workflows/ci.yml:
    - name: CI
    - on: [push, pull_request] (per D-10)
    - Job 1 "lint": runs-on ubuntu-latest, python-version "3.11" (single version for speed), steps: checkout@v4, setup-python@v5, pip install -e '.[dev]', ruff check ., ruff format --check ., mypy shipping_tracker/ --strict
    - Job 2 "test": runs-on ubuntu-latest, strategy.matrix.python-version: ["3.11","3.12","3.13"] (per D-10), steps: checkout@v4, setup-python@v5, pip install -e '.[dev]', pytest

    Create the .github/workflows/ directory path if it does not exist (the ci.yml file creation creates it).
  </action>
  <verify>
    <automated>python -c "import yaml; yaml.safe_load(open('.pre-commit-config.yaml'))" 2>/dev/null || python -c "import json; print('yaml check skipped — verify YAML manually')" && python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" 2>/dev/null || python -c "print('yaml check skipped')" && pre-commit run --all-files</automated>
  </verify>
  <acceptance_criteria>
    - .pre-commit-config.yaml is valid YAML and contains ruff-pre-commit and mirrors-mypy repos
    - .pre-commit-config.yaml hook order: ruff-check appears before ruff-format
    - .pre-commit-config.yaml mypy hook has additional_dependencies listing httpx, structlog, and python-dotenv
    - .pre-commit-config.yaml rev for ruff-pre-commit is v0.15.15 and for mirrors-mypy is v2.1.0
    - .github/workflows/ci.yml is valid YAML
    - .github/workflows/ci.yml triggers on [push, pull_request]
    - .github/workflows/ci.yml test job matrix contains "3.11", "3.12", "3.13"
    - .github/workflows/ci.yml lint job runs mypy shipping_tracker/ --strict
    - pre-commit run --all-files exits 0 (both ruff and mypy hooks pass)
  </acceptance_criteria>
  <done>pre-commit hooks are configured and pass. GitHub Actions CI workflow is present with the correct matrix. Both files are valid YAML.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test infrastructure — conftest, fixtures directory, and smoke tests (SETUP-04)</name>
  <files>tests/__init__.py, tests/conftest.py, tests/fixtures/.gitkeep, tests/test_smoke.py</files>
  <read_first>
    - .planning/phases/01-scaffold/01-PATTERNS.md §tests/conftest.py — synthetic fixture pattern; privacy docstring is mandatory
    - .planning/phases/01-scaffold/01-RESEARCH.md §Code Examples (pytest fixture pattern) — synthetic data rules: FAKE prefix, no real addresses
    - .planning/phases/01-scaffold/01-VALIDATION.md §Per-Task Verification Map — smoke tests must cover: importability (SETUP-01), entry point exits 0 (SETUP-01), pytest discovers suite (SETUP-04)
    - .planning/phases/01-scaffold/01-CONTEXT.md §Privacy & Security — synthetic data only, privacy constraint is non-negotiable
    - pyproject.toml (created in Plan 01) — [tool.pytest.ini_options] testpaths = ["tests"], pythonpath = ["."]
  </read_first>
  <behavior>
    - test_package_importable: import shipping_tracker succeeds without ImportError
    - test_parsers_subpackage_importable: import shipping_tracker.parsers.base and from shipping_tracker.parsers.base import BaseParser, TrackingInfo both succeed
    - test_base_parser_is_abstract: attempting to instantiate BaseParser() directly raises TypeError (cannot instantiate abstract class)
    - test_tracking_info_dataclass: TrackingInfo(tracking_number="FAKE123", carrier="FAKECARRIER") creates an instance with correct field values
    - test_entry_point_exits_zero: subprocess.run(["python", "-m", "shipping_tracker"], capture_output=True) returns returncode == 0
    - test_entry_point_no_stdout: the subprocess above produces empty stdout (cron silence — per D-07 / LOG-03)
  </behavior>
  <action>
    Create tests/__init__.py as an empty file (makes tests/ a package for consistent pytest import resolution).

    Create tests/conftest.py using the pattern from PATTERNS.md §tests/conftest.py:
    - Module docstring with the mandatory privacy guardrail comment (copy exact wording from PATTERNS.md: "PRIVACY: All fixtures use synthetic data. No real tracking numbers, email addresses, order IDs, or personal names may appear...")
    - Import pytest
    - @pytest.fixture def synthetic_email_body() -> str: returns the three-line synthetic body with "FAKE1234567890" and "FAKECARRIER" — these are clearly synthetic; no real data

    Create tests/fixtures/.gitkeep: empty file to track the fixtures/ directory in git (the directory itself would otherwise be ignored by git).

    Create tests/test_smoke.py with all six behaviors listed above:
    - import subprocess at module level; from shipping_tracker.parsers.base import BaseParser, TrackingInfo
    - test_package_importable: import shipping_tracker (assert the module has a __name__ attribute)
    - test_parsers_subpackage_importable: from shipping_tracker.parsers.base import BaseParser, TrackingInfo; assert both are not None
    - test_base_parser_is_abstract: use pytest.raises(TypeError) when calling BaseParser()
    - test_tracking_info_dataclass: ti = TrackingInfo(tracking_number="FAKE123", carrier="FAKECARRIER"); assert ti.tracking_number == "FAKE123"; assert ti.carrier == "FAKECARRIER"
    - test_entry_point_exits_zero: result = subprocess.run(["python", "-m", "shipping_tracker"], capture_output=True); assert result.returncode == 0
    - test_entry_point_no_stdout: same result object; assert result.stdout == b"" (cron silence — D-07)

    All test data must use synthetic values only (FAKE prefix for tracking numbers, FAKECARRIER for carrier). No real email addresses or personal data anywhere in test files.
  </action>
  <verify>
    <automated>pytest -v && ruff check . && mypy shipping_tracker/ --strict</automated>
  </verify>
  <acceptance_criteria>
    - pytest -v exits 0 with exactly 6 tests collected and 6 passed, 0 failed
    - tests/conftest.py contains "FAKE1234567890" and the privacy docstring warning
    - tests/fixtures/.gitkeep exists and is tracked by git (git ls-files tests/fixtures/.gitkeep outputs the path)
    - tests/test_smoke.py contains test_entry_point_exits_zero and test_entry_point_no_stdout
    - tests/test_smoke.py contains no real email addresses, tracking numbers, or order references
    - ruff check . exits 0 (test files also pass linting)
    - mypy shipping_tracker/ --strict exits 0 (test files are not under shipping_tracker/ so not mypy-checked, but source remains clean)
  </acceptance_criteria>
  <done>pytest discovers and runs 6 smoke tests, all pass. Test infrastructure is in place for Phases 2–5. Fixtures directory is tracked in git. Privacy constraint satisfied — all test data is synthetic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → test suite | Test data must never contain PII; synthetic boundary enforced |
| CI workflow → GitHub Actions | CI config is committed; must not embed secrets or credentials |
| pre-commit → developer machine | Hooks run in isolated virtualenv; must have required type stubs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | tests/conftest.py and tests/fixtures/ | mitigate | Privacy docstring in conftest.py enforces the rule; all test data uses FAKE prefix; no real email addresses, tracking numbers, or order IDs in any test file |
| T-02-02 | Information Disclosure | .github/workflows/ci.yml | mitigate | No secrets embedded in workflow YAML; secrets are managed via GitHub repository secrets (Phase 5 concern); Phase 1 CI requires no credentials |
| T-02-03 | Tampering | pre-commit hooks | mitigate | ruff-pre-commit and mirrors-mypy pinned to exact revs (v0.15.15, v2.1.0); additional_dependencies pinned to ensure reproducible mypy checks |
| T-02-SC | Tampering | pip install (pre-commit hook isolation) | accept | pre-commit's isolated virtualenv installs from the pinned rev and additional_dependencies; all packages already cleared in Plan 01 Package Legitimacy Audit |
</threat_model>

<verification>
After both tasks complete, run the full Phase 1 gate check from the repo root:

```
pip install -e ".[dev]" && ruff check . && ruff format --check . && mypy shipping_tracker/ --strict && pytest -v && python -m shipping_tracker && pre-commit run --all-files && git check-ignore .env
```

Expected results:
- ruff check: zero violations
- ruff format --check: zero violations
- mypy --strict: "Success: no issues found"
- pytest -v: 6 passed, 0 failed
- python -m shipping_tracker: exits 0, empty stdout
- pre-commit run --all-files: all hooks pass
- git check-ignore .env: outputs ".env"

All Phase 1 success criteria from ROADMAP.md are now satisfied.
</verification>

<success_criteria>
1. pytest runs and passes all 6 smoke tests — zero failures, zero errors (SETUP-04)
2. pre-commit run --all-files exits 0 — ruff and mypy hooks both green (SETUP-05)
3. .github/workflows/ci.yml present with push + pull_request triggers and 3.11/3.12/3.13 matrix (SETUP-06)
4. tests/fixtures/ directory is tracked in git (fixtures/.gitkeep committed)
5. No PII in any test file — all tracking numbers use FAKE prefix, no real email addresses
6. Full gate passes: ruff + mypy --strict + pytest + python -m shipping_tracker all exit 0
</success_criteria>

<output>
Create `.planning/phases/01-scaffold/01-02-SUMMARY.md` when done.
</output>
