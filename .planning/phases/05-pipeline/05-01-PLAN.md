---
phase: 05-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/conftest.py
  - tests/test_registrar.py
autonomous: true
requirements: [TRACK-01, TRACK-02, TRACK-03, TRACK-04, TRACK-05]
must_haves:
  truths:
    - "Running pytest tests/test_registrar.py collects all TRACK-01..05 behavior tests"
    - "Every TRACK-01..05 test fails RED with ImportError/AttributeError until source lands (registrar.TrackingMoreRegistrar / QuotaExceededError do not yet exist)"
    - "respx is an installable dev dependency and importable in the test environment"
    - "No test makes a live network call (respx intercepts every httpx request)"
  artifacts:
    - path: "tests/test_registrar.py"
      provides: "Failing RED test suite covering TRACK-01..05 + LOG-02/D-05/D-06"
      min_lines: 180
      contains: "def test_success_creates_tracking"
    - path: "tests/conftest.py"
      provides: "mock_router respx fixture + synthetic TrackingMore response builders"
      contains: "def mock_router"
    - path: "pyproject.toml"
      provides: "respx dev dependency"
      contains: "respx"
  key_links:
    - from: "tests/test_registrar.py"
      to: "shipping_tracker.registrar.TrackingMoreRegistrar"
      via: "import (fails RED until Plan 02)"
      pattern: "from shipping_tracker.registrar import .*TrackingMoreRegistrar"
    - from: "tests/conftest.py"
      to: "respx.MockRouter"
      via: "mock_router fixture"
      pattern: "respx\\.MockRouter"
---

<objective>
Author the Nyquist Wave 0 test scaffold for the TrackingMore pipeline slice BEFORE
any source is written. This plan adds the `respx` dev dependency, the `mock_router`
respx fixture plus synthetic TrackingMore response builders in `tests/conftest.py`,
and a complete `tests/test_registrar.py` that exercises every TRACK-01..05 behavior
(plus LOG-02 / D-05 / D-06 regression checks) against a mocked HTTP transport.

This is the MVP "failing test first" task: the suite encodes the full end-to-end
happy path (parsed tracking number → confirmed registration → both DB rows written)
and every error path, and it MUST fail RED — `TrackingMoreRegistrar` and
`QuotaExceededError` do not exist until Plan 02 (Wave 2) lands.

Purpose: Satisfy Nyquist Dimension 8 — every TRACK requirement has an automated
`<verify>` that exists before the code it tests. Zero live calls (D-04); CI needs
no secret.
Output: `respx` in `pyproject.toml`, fixtures/builders in `tests/conftest.py`,
and the RED `tests/test_registrar.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pipeline/05-CONTEXT.md
@.planning/phases/05-pipeline/05-RESEARCH.md
@.planning/phases/05-pipeline/05-PATTERNS.md
@.planning/phases/05-pipeline/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add respx dev dependency and conftest mock_router fixture + response builders</name>
  <files>pyproject.toml, tests/conftest.py</files>
  <read_first>
    - pyproject.toml (see current `[project.optional-dependencies] dev` list — append, do not reorder existing entries)
    - tests/conftest.py (mirror the existing db_conn fixture style + PRIVACY docstring convention)
    - .planning/phases/05-pipeline/05-PATTERNS.md (sections "tests/conftest.py (modify...)" and "pyproject.toml (modify...)" — exact builder bodies and import additions)
    - .planning/phases/05-pipeline/05-RESEARCH.md (Standard Stack: respx>=0.23; Response → Outcome Mapping Table for exact meta.code values in each builder)
  </read_first>
  <action>
    Add `"respx>=0.23"` to the `dev` list in `[project.optional-dependencies]` of
    pyproject.toml (append a new entry; leave existing ruff/mypy/pytest/pre-commit/
    google-api-python-client-stubs entries untouched). respx>=0.23 is compatible with
    the installed httpx 0.28.1 (respx requires httpx>=0.25).

    Then run `pip install -e ".[dev]"` (or `pip install respx>=0.23`) so respx is
    importable in the active environment — the RED tests in Task 2 import respx at
    collection time and will error on collection if it is absent.

    In tests/conftest.py add `import httpx` and `import respx` to the import block,
    then add a `mock_router` pytest fixture returning `respx.MockRouter()` with a
    PRIVACY docstring matching the existing db_conn convention (note: zero live calls,
    all HTTP intercepted by respx). Also add module-level synthetic response-builder
    helper functions (NOT fixtures): `make_success_response()` → httpx.Response(200,
    json meta.code 200), `make_already_exists_response()` → httpx.Response(400, json
    meta.code 4016), `make_quota_response()` → httpx.Response(400, json meta.code 4021),
    `make_rate_limit_response()` → httpx.Response(429, json meta.code 429),
    `make_5xx_response()` → httpx.Response(500, json meta.code 500). Use only the exact
    meta.code integers from the RESEARCH Response → Outcome Mapping Table. All response
    bodies are synthetic — no real tracking data.
  </action>
  <verify>
    <automated>python -c "import respx; import tests.conftest as c; assert hasattr(c, 'make_success_response') and hasattr(c, 'make_already_exists_response') and hasattr(c, 'make_quota_response') and hasattr(c, 'make_rate_limit_response') and hasattr(c, 'make_5xx_response')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import respx"` exits 0 (respx installed)
    - `pyproject.toml` contains the string `respx` inside the `dev` list
    - `tests/conftest.py` defines a `mock_router` fixture that returns `respx.MockRouter()`
    - `tests/conftest.py` defines all five builders: make_success_response, make_already_exists_response, make_quota_response, make_rate_limit_response, make_5xx_response
    - make_success_response returns status 200 with json `{"meta": {"code": 200, ...}, ...}`; make_already_exists_response uses meta.code 4016; make_quota_response uses meta.code 4021; make_rate_limit_response uses status 429
    - No real tracking numbers, emails, or order refs appear in any builder body
  </acceptance_criteria>
  <done>respx is an installed dev dependency; conftest exposes a mock_router fixture and five synthetic TrackingMore response builders with the exact RESEARCH meta.code values.</done>
</task>

<task type="auto">
  <name>Task 2: Author the RED test_registrar.py suite covering TRACK-01..05 + LOG-02/D-05/D-06</name>
  <files>tests/test_registrar.py</files>
  <read_first>
    - tests/test_db.py (analog — file header, section-header comment style, inline callable conventions, caplog regression pattern)
    - tests/conftest.py (the mock_router fixture + builders added in Task 1, and the db_conn fixture)
    - tests/fixtures/fake_db.py (reuse FAKE_MESSAGE_ID_1, FAKE_MESSAGE_ID_2, FAKE_TRACKING_NUMBER_1, FAKE_TRACKING_NUMBER_2 — do NOT redefine)
    - shipping_tracker/db.py (register_and_persist signature — Plan 02 adds a carrier param; integration tests call it)
    - shipping_tracker/main.py (the dispatch loop + WR-04 broad except at line 135 — the D-06 ordering test asserts QuotaExceededError breaks the loop)
    - .planning/phases/05-pipeline/05-VALIDATION.md (Per-Task Verification Map — the exact test function names each row expects)
    - .planning/phases/05-pipeline/05-PATTERNS.md (section "tests/test_registrar.py (new file)" — header, _make_registrar helper, respx mock construction, timeout side_effect, LOG-02 regression pattern)
    - .planning/phases/05-pipeline/05-RESEARCH.md (Response → Outcome Mapping Table + Code Examples — drives every expected outcome)
  </read_first>
  <action>
    Create tests/test_registrar.py with the synthetic-only privacy header. Import
    httpx, respx, pytest, sqlite3; import register_and_persist from shipping_tracker.db;
    import QuotaExceededError and TrackingMoreRegistrar from shipping_tracker.registrar
    (these imports fail RED until Plan 02 — that is correct and required). Reuse the
    FAKE_* constants from tests.fixtures.fake_db. Add a `_make_registrar(router)` helper
    that builds `TrackingMoreRegistrar(api_key="FAKE_KEY", client=httpx.Client(transport=router))`.

    Author EXACTLY these test functions (names are contract-bound to 05-VALIDATION.md
    and must match precisely):
    - TRACK-01: `test_create_sends_correct_request` — asserts POST goes to
      https://api.trackingmore.com/v4/trackings/create and the request carries header
      `Tracking-Api-Key` equal to the injected key (inspect the recorded respx request).
    - TRACK-01: `test_success_creates_tracking` — mock 200/meta.code 200; via
      register_and_persist over db_conn → returns True; assert BOTH rows present
      (processed_emails + registered_tracking) using the db helpers.
    - TRACK-02: `test_api_key_in_header` — request header `Tracking-Api-Key` == "FAKE_KEY".
    - TRACK-02: `test_missing_api_key_exits_1` — call shipping_tracker.main.main() with
      monkeypatched env so TRACKINGMORE_API_KEY is unset/empty; assert return value == 1
      AND that no Gmail fetch / DB work happened (monkeypatch fetch_unread_shipping_emails
      to raise if called, asserting it is never invoked). (This drives D-05.)
    - TRACK-03: `test_already_exists_treated_as_success` — mock 400/meta.code 4016 →
      register_and_persist returns True; both rows written.
    - TRACK-04: `test_rate_limit_raises_quota_error` — mock 429 → calling the registrar
      raises QuotaExceededError.
    - TRACK-04: `test_quota_exhausted_raises_quota_error` — mock 400/meta.code 4021 →
      raises QuotaExceededError.
    - TRACK-04: `test_quota_error_breaks_dispatch_loop` — integration: with two pending
      synthetic numbers and the registrar raising QuotaExceededError on the first, assert
      main()'s loop breaks so the SECOND number is NOT written to registered_tracking
      (retry-next-run proof, DEDUP-05 path).
    - TRACK-04: `test_5xx_retries_once_then_defers` — mock 500 twice → registrar returns
      False after one retry; the number is NOT in registered_tracking. Inject
      retry_pause=0 (or monkeypatch time.sleep) so the test does not actually pause.
    - TRACK-04: `test_timeout_retries_once_then_defers` — side_effect httpx.TimeoutException
      on both attempts → returns False; number not persisted; one retry occurred.
    - TRACK-05: `test_no_courier_code_when_carrier_none` — call registrar with carrier=None;
      assert recorded request body has key `tracking_number` and NO `courier_code` key (D-08).
    - TRACK-05: `test_courier_code_included_when_carrier_set` — carrier="aliexpress";
      assert request body includes `courier_code` == "aliexpress".
    - LOG-02: `test_tracking_number_never_logged` — with caplog, exercise success + error
      paths; assert FAKE_TRACKING_NUMBER_1 appears in no caplog record message.
    - D-05: `test_api_key_never_logged` — with caplog, drive the missing-key path and an
      error path; assert "FAKE_KEY" / the key value appears in no caplog record.
    - D-06: `test_quota_error_ordering` — assert QuotaExceededError is NOT a subclass that
      would be swallowed incorrectly and that when raised through register_and_persist it
      propagates (db.py re-raises) rather than being caught — proving main() can break.

    Use section-header comment banners (`# ----`) per requirement, matching test_db.py.
    Mock every transport via respx — NEVER let a real request escape. For transient/timeout
    tests, set retry_pause=0 or monkeypatch time.sleep to keep runtime <5s. Do not assert on
    exact log message wording beyond PII-absence (keeps tests robust to D-07 phrasing).
  </action>
  <verify>
    <automated>python -m pytest tests/test_registrar.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_registrar.py --collect-only -q` lists all 15 named test functions (test_create_sends_correct_request, test_success_creates_tracking, test_api_key_in_header, test_missing_api_key_exits_1, test_already_exists_treated_as_success, test_rate_limit_raises_quota_error, test_quota_exhausted_raises_quota_error, test_quota_error_breaks_dispatch_loop, test_5xx_retries_once_then_defers, test_timeout_retries_once_then_defers, test_no_courier_code_when_carrier_none, test_courier_code_included_when_carrier_set, test_tracking_number_never_logged, test_api_key_never_logged, test_quota_error_ordering) — OR collection errors solely on the `from shipping_tracker.registrar import ... TrackingMoreRegistrar` import (acceptable RED state since source lands in Plan 02)
    - `pytest tests/test_registrar.py -x -q` fails RED with ImportError/AttributeError on TrackingMoreRegistrar / QuotaExceededError (source does not yet exist)
    - The file imports FAKE_MESSAGE_ID_1 and FAKE_TRACKING_NUMBER_1 from tests.fixtures.fake_db (no duplicate constant definitions)
    - No test issues a live network request — every test uses the mock_router / respx transport or monkeypatched fetch
    - No real tracking numbers, email addresses, or order references appear anywhere in the file (grep for `@` and real-looking tracking shapes returns only FAKE-prefixed synthetic values)
  </acceptance_criteria>
  <done>tests/test_registrar.py exists with all 15 contract-named tests, fails RED on the missing TrackingMoreRegistrar/QuotaExceededError import, uses only synthetic FAKE data, and makes zero live calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → PyPI (pip install respx) | Untrusted package install crosses here during dev-dependency setup |
| test code → (mocked) TrackingMore API | respx intercepts; no real boundary is crossed at test time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Test fixtures / response builders | mitigate | All builder bodies use synthetic data only (no real tracking numbers/emails); LOG-02 regression tests assert no PII in logs |
| T-05-02 | Information Disclosure | API key in tests | mitigate | Tests use `api_key="FAKE_KEY"`; `test_api_key_never_logged` asserts the key value never reaches log output |
| T-05-SC | Tampering | `respx` PyPI install | accept | respx audited `[OK]` in 05-RESEARCH.md Package Legitimacy Audit (github.com/lundberg/respx, ~5yr, ~325k/wk); no `[ASSUMED]`/`[SUS]` packages — no human checkpoint required |
</threat_model>

<verification>
- `python -c "import respx"` exits 0
- `pytest tests/test_registrar.py --collect-only -q` enumerates the 15 contract tests (or errors only on the not-yet-existing TrackingMoreRegistrar import)
- `pytest tests/test_registrar.py -x -q` is RED on the missing source symbols (expected before Plan 02)
- `pytest -x -q` for the rest of the suite still passes (this plan adds only new tests + a fixture, touches no source)
</verification>

<success_criteria>
- respx>=0.23 is a declared, installed dev dependency
- tests/conftest.py exposes mock_router + five synthetic response builders
- tests/test_registrar.py contains all 15 contract-named tests, all RED on missing source
- Zero live network calls; zero PII; all data FAKE-prefixed
</success_criteria>

<output>
Create `.planning/phases/05-pipeline/05-01-SUMMARY.md` when done
</output>
