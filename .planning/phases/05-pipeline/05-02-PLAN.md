---
phase: 05-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - shipping_tracker/registrar.py
  - shipping_tracker/db.py
  - shipping_tracker/main.py
autonomous: true
requirements: [TRACK-01, TRACK-02, TRACK-03, TRACK-04, TRACK-05]
must_haves:
  truths:
    - "A parsed tracking number not yet in the DB is registered via POST https://api.trackingmore.com/v4/trackings/create and written to registered_tracking only on confirmed success (TRACK-01)"
    - "TRACKINGMORE_API_KEY is read only from the env var and the key value is never logged; missing/empty key makes main() return 1 before any Gmail/DB work (TRACK-02, D-05)"
    - "A 4016 already-exists response is treated as success and persisted, not raised (TRACK-03)"
    - "A 429 rate-limit or 4021 quota response raises QuotaExceededError; main()'s loop catches it before the broad except and breaks, leaving remaining numbers unwritten for next-run retry (TRACK-04, D-01, D-06)"
    - "A 5xx/timeout transient failure triggers exactly one ~2s retry, then defers without persisting (TRACK-04, D-02)"
    - "carrier=None omits courier_code entirely; a non-empty carrier is sent as courier_code; registration never requires it (TRACK-05, D-08)"
    - "main() owns the httpx.Client lifetime: a named http_client local is constructed, injected into TrackingMoreRegistrar, and closed in the finally block alongside conn.close() (Pitfall 4)"
  artifacts:
    - path: "shipping_tracker/registrar.py"
      provides: "TrackingMoreRegistrar (injectable httpx.Client) + QuotaExceededError"
      contains: "class TrackingMoreRegistrar"
      exports: ["TrackingMoreRegistrar", "QuotaExceededError", "Registrar", "NullRegistrar"]
    - path: "shipping_tracker/db.py"
      provides: "register_and_persist carrier passthrough"
      contains: "carrier"
    - path: "shipping_tracker/main.py"
      provides: "D-05 key check, TrackingMoreRegistrar wiring, QuotaExceededError catch before broad except, named httpx.Client closed in finally"
      contains: "TrackingMoreRegistrar"
  key_links:
    - from: "shipping_tracker/main.py"
      to: "shipping_tracker.registrar.TrackingMoreRegistrar"
      via: "http_client = httpx.Client(timeout=10.0); registrar: Registrar = TrackingMoreRegistrar(api_key=api_key, client=http_client)"
      pattern: "TrackingMoreRegistrar\\("
    - from: "shipping_tracker/main.py"
      to: "shipping_tracker.registrar.QuotaExceededError"
      via: "except QuotaExceededError before broad except Exception"
      pattern: "except QuotaExceededError"
    - from: "shipping_tracker/main.py"
      to: "http_client.close()"
      via: "finally block closes the named http_client alongside conn.close()"
      pattern: "http_client\\.close\\(\\)"
    - from: "shipping_tracker/registrar.py"
      to: "https://api.trackingmore.com/v4/trackings/create"
      via: "client.post"
      pattern: "v4/trackings/create"
    - from: "shipping_tracker/db.py"
      to: "shipping_tracker/main.py"
      via: "register_and_persist(..., carrier=result.carrier)"
      pattern: "register_and_persist\\("
---

<objective>
Implement the complete TrackingMore pipeline slice that turns the Plan 01 RED suite
GREEN: add `TrackingMoreRegistrar` (injectable `httpx.Client`, D-04) and
`QuotaExceededError` to `registrar.py`, thread `carrier` through
`db.register_and_persist`, and make the three surgical `main.py` edits — the D-05
API-key fail-fast, swapping `NullRegistrar` for `TrackingMoreRegistrar`, and adding
the `except QuotaExceededError: ... break` clause BEFORE the broad `except Exception`
(D-06 CRITICAL ordering). After this plan, Gmail fetch → parse → deduplicate →
register runs end-to-end against a (mocked-in-tests, real-in-prod) TrackingMore API.

This is the MVP vertical slice: the thinnest UI/API/DB path that makes the failing
test pass — a parsed tracking number now produces a confirmed registration and a
persisted DB row, with every error path (already-exists, quota/429, transient, bad
courier) mapped onto the locked Phase 4 persistence semantics.

Purpose: Close TRACK-01..05 and complete the Phase 5 end-to-end run.
Output: Working TrackingMoreRegistrar wired into main(); full Plan 01 suite GREEN;
mypy --strict and ruff clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pipeline/05-CONTEXT.md
@.planning/phases/05-pipeline/05-RESEARCH.md
@.planning/phases/05-pipeline/05-PATTERNS.md
@.planning/phases/05-pipeline/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add QuotaExceededError and TrackingMoreRegistrar to registrar.py</name>
  <files>shipping_tracker/registrar.py</files>
  <behavior>
    - 200/201 with meta.code 200 → __call__ returns True, logs INFO "registrar.created" (no PII)
    - meta.code 4016 (and defensive 4101) → returns True, logs "registrar.already_exists" (TRACK-03 success)
    - HTTP 429 → raises QuotaExceededError("rate-limit")
    - meta.code 4021 (and defensive 4190) or HTTP 402 → raises QuotaExceededError("quota-exhausted")
    - 5xx → one retry after retry_pause; if still 5xx, raises a structural-message httpx error (no PII) handled as transient
    - httpx.TimeoutException / httpx.ConnectError → one retry after retry_pause, then return False
    - carrier=None/empty → request body omits courier_code; non-empty carrier → courier_code present
    - Tracking-Api-Key header equals the injected api_key on every request
    - No exception message or log line ever contains tracking_number, carrier, or the api_key value (LOG-02)
  </behavior>
  <read_first>
    - shipping_tracker/registrar.py (existing module — keep the docstring, Registrar Protocol, NullRegistrar; ADD to it)
    - .planning/phases/05-pipeline/05-PATTERNS.md (section "shipping_tracker/registrar.py (modify...)" — exact import additions, QuotaExceededError placement, TrackingMoreRegistrar + _handle bodies)
    - .planning/phases/05-pipeline/05-RESEARCH.md (Response → Outcome Mapping Table; Pattern 1 + Pattern 2 code; Pitfall 6 defensive json parsing; Open Questions Q-1/Q-2 RESOLUTIONS — courier-required rejections and missing meta.code both fall through to the other-4xx / transient paths, no extra branch needed)
    - tests/test_registrar.py (the RED contract from Plan 01 — implement to make these exact tests pass)
  </read_first>
  <action>
    Append `import time` and `import httpx` to the existing import block. Define
    `QuotaExceededError(Exception)` immediately after the module `logger`, before the
    Registrar Protocol, with a docstring noting LOG-02 (no PII in message) and that
    main() catches it before the broad except (D-06). Add module-level
    `_BASE_URL = "https://api.trackingmore.com"`.

    Add `class TrackingMoreRegistrar` implementing the Registrar Protocol. Constructor:
    `__init__(self, api_key: str, client: httpx.Client | None = None, *, retry_pause: float = 2.0)`
    storing `self._api_key`, `self._client = client or httpx.Client(timeout=10.0)` (D-03
    10s timeout), and `self._retry_pause`. `__call__(self, tracking_number, carrier) -> bool`
    builds `payload = {"tracking_number": tracking_number}` and adds
    `payload["courier_code"] = carrier` only when `carrier` is truthy (D-08 — omit the key
    entirely otherwise, never send null). Loop `for attempt in range(2)`: POST
    `f"{_BASE_URL}/v4/trackings/create"` with `json=payload` and header
    `{"Tracking-Api-Key": self._api_key}` (TRACK-02); on `httpx.TimeoutException` /
    `httpx.ConnectError`, if attempt==0 sleep `self._retry_pause` and continue (D-02 single
    retry), else return False; otherwise return `self._handle(resp)`.

    `_handle(self, resp) -> bool`: if status_code == 429 raise QuotaExceededError("rate-limit")
    (D-06, no PII). Parse the body defensively (try resp.json() except → {} per Pitfall 6 —
    this is the RESEARCH Q-2 RESOLUTION: an absent/non-JSON meta yields meta_code None and
    falls through to the status-code checks below, no crash). Read
    `meta_code = body.get("meta", {}).get("code")`. Mapping (exact codes from RESEARCH
    table): 200/201 → log INFO "registrar.created", return True; 4016/4101 → log INFO
    "registrar.already_exists", return True (TRACK-03); 4021/4190 or status 402 → raise
    QuotaExceededError("quota-exhausted") (D-01/D-06); status_code >= 500 → raise
    httpx.HTTPStatusError("server-error", request=resp.request, response=resp) so __call__'s
    transient retry path handles it (NO tracking_number in the message); any other 4xx → log
    ERROR "registrar.error code=%s" with meta_code only, return False. NOTE (RESEARCH Q-1
    RESOLUTION): a courier-required rejection (4013/4015 or any other non-4016/non-4021 4xx)
    is intentionally NOT special-cased — it lands on this generic other-4xx → return False
    branch, which defers the number for next-run retry (DEDUP-05). This honors D-08 as locked
    without a dedicated detect call or extra test. Every log call and every exception string
    carries only structural data — never tracking_number, carrier, or the key (LOG-02). Keep
    mypy --strict clean: annotate payload as `dict[str, str]`, add the trailing unreachable
    `return False` the type checker needs.
  </action>
  <verify>
    <automated>python -m pytest tests/test_registrar.py -k "create or already_exists or rate_limit or quota_exhausted or retries or courier or api_key or tracking_number_never" --deselect tests/test_registrar.py::test_quota_error_breaks_dispatch_loop --deselect tests/test_registrar.py::test_quota_error_ordering -q && python -m mypy --strict shipping_tracker/registrar.py</automated>
  </verify>
  <acceptance_criteria>
    - `shipping_tracker/registrar.py` defines `class QuotaExceededError(Exception)` and `class TrackingMoreRegistrar`
    - The existing `Registrar` Protocol and `NullRegistrar` are unchanged and still present
    - `grep -n "v4/trackings/create" shipping_tracker/registrar.py` matches
    - `grep -n "Tracking-Api-Key" shipping_tracker/registrar.py` matches
    - Source assertion: courier_code is added under an `if carrier` guard (no unconditional courier_code; no literal `None` sent)
    - `mypy --strict shipping_tracker/registrar.py` exits 0
    - The Plan 01 unit tests for create/already-exists/rate-limit/quota-exhausted/retries/courier/api-key/log-pii pass (the two dispatch-loop integration tests — test_quota_error_breaks_dispatch_loop, test_quota_error_ordering — are EXPECTED to remain RED at this checkpoint; they only go GREEN after Task 2 wires main(), which is why the verify command deselects them and uses `quota_exhausted` rather than bare `quota`)
    - Source assertion: no f-string or log call in the file interpolates `tracking_number`, `carrier`, or `self._api_key` (grep finds no `tracking_number` or `api_key` inside any `logger.` or `raise` argument)
  </acceptance_criteria>
  <done>registrar.py exports TrackingMoreRegistrar + QuotaExceededError mapping all six response outcomes onto bool/raise per the RESEARCH table, fully typed, PII-safe; the registrar-only Plan 01 tests are GREEN (the two main()-dependent dispatch-loop tests stay RED until Task 2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread carrier through db.register_and_persist and wire main() (D-05 check, registrar swap, D-06 catch, named http_client closed in finally)</name>
  <files>shipping_tracker/db.py, shipping_tracker/main.py</files>
  <behavior>
    - register_and_persist accepts an optional carrier and passes it to registrar(tracking_number, carrier) instead of hardcoded None; QuotaExceededError still propagates (db re-raises)
    - main() returns 1 immediately when TRACKINGMORE_API_KEY is missing/empty, before fetch_unread_shipping_emails or any DB open, and never logs the key value
    - main() constructs a named local `http_client = httpx.Client(timeout=10.0)`, injects it as `TrackingMoreRegistrar(api_key=api_key, client=http_client)`, and closes `http_client` in the finally block alongside `conn.close()`
    - On QuotaExceededError from register_and_persist, main() logs one WARNING "registrar.quota_exceeded" and breaks the loop; the broad except Exception still handles all other errors and remains AFTER the QuotaExceededError clause
    - The second pending number in a quota-short-circuit run is never written to registered_tracking
  </behavior>
  <read_first>
    - shipping_tracker/db.py (register_and_persist at lines 67-103 — add carrier param, change the hardcoded `registrar(tracking_number, None)` call; the `except Exception: raise` already propagates QuotaExceededError, leave it)
    - shipping_tracker/main.py (full file — line 22 registrar import, line 64-65 load_dotenv/configure_logging, line 69 DB open, line 73 NullRegistrar, lines 129-131 register_and_persist call, lines 135-146 the WR-04 broad except)
    - .planning/phases/05-pipeline/05-PATTERNS.md (sections "shipping_tracker/main.py (modify...)" and "shipping_tracker/db.py (modify...)" — exact edit locations, the CRITICAL except-ordering note, AND the "Lifetime management" block showing the named `http_client = httpx.Client(...)` / inject / `http_client.close()` in finally pattern — follow it exactly)
    - .planning/phases/05-pipeline/05-CONTEXT.md (D-01, D-05, D-06, D-07, D-08 — the locked semantics and the CRITICAL wiring note)
    - tests/test_registrar.py (test_missing_api_key_exits_1 and test_quota_error_breaks_dispatch_loop — the integration contract this task satisfies)
  </read_first>
  <action>
    In db.py: add a trailing parameter `carrier: str | None = None` to register_and_persist
    and change `success = registrar(tracking_number, None)` to
    `success = registrar(tracking_number, carrier)`. Update the docstring to note carrier is
    fed from TrackingInfo.carrier (D-08). Do NOT touch the `except Exception: raise` — it must
    keep propagating QuotaExceededError so main() can break (D-06).

    In main.py: (1) widen the registrar import on line 22 to
    `from shipping_tracker.registrar import NullRegistrar, QuotaExceededError, Registrar, TrackingMoreRegistrar`
    and add `import httpx` to the stdlib import block. (2) Immediately after
    `configure_logging()` (before the DB open), add the D-05 fail-fast:
    `api_key = os.getenv("TRACKINGMORE_API_KEY", "").strip()`; if falsy, `logger.error("config.missing_api_key")`
    (never log the key value) and `return 1` — this runs before fetch and before sqlite3.connect.
    (3) Construct the registrar with a NAMED httpx.Client local so its lifetime is owned by
    main() and can be closed: assign `http_client = httpx.Client(timeout=10.0)` as a named local
    variable FIRST, then build `registrar: Registrar = TrackingMoreRegistrar(api_key=api_key,
    client=http_client)` — do NOT inline `httpx.Client(...)` inside the TrackingMoreRegistrar(...)
    call (there would be no variable to close). Replace the existing
    `registrar: Registrar = NullRegistrar()` (line 73) with this named-client construction. In the
    `finally` block, close `http_client` alongside the existing `conn.close()` — i.e.
    `conn.close()` then `http_client.close()` (Pitfall 4: main() owns and closes the connection
    pool). Follow the PATTERNS.md "Lifetime management" block verbatim. (4) Pass the parsed
    carrier through the call: `register_and_persist(conn, email.message_id,
    result.tracking_number, registrar, carrier=result.carrier)`. (5) Add the
    `except QuotaExceededError:` clause that logs one WARNING `"registrar.quota_exceeded"` (D-07,
    no PII) and `break`s — placed IMMEDIATELY BEFORE the existing `except Exception as exc:` broad
    handler (D-06 CRITICAL: Python evaluates except clauses in order; the broad catch must stay
    after it or it swallows QuotaExceededError and the loop continues instead of stopping). Leave
    the broad except body (WR-04 message_id + type(exc).__name__ logging) untouched.
  </action>
  <verify>
    <automated>python -m pytest tests/test_registrar.py -q && python -m mypy --strict shipping_tracker/ && python -m ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_registrar.py -q` is fully GREEN (all 15 tests pass)
    - `pytest -x -q` (full suite) passes
    - `mypy --strict shipping_tracker/` exits 0
    - `ruff check .` exits 0
    - Source assertion: in main.py the `except QuotaExceededError` clause appears at a lower line number than `except Exception` within the dispatch loop (ordering correct, D-06)
    - Source assertion: main.py reads `os.getenv("TRACKINGMORE_API_KEY", ...)` and `return 1` before the `sqlite3.connect(` call and before `fetch_unread_shipping_emails(`
    - Source assertion: db.py `register_and_persist` signature includes `carrier` and the registrar call passes `carrier` (not literal `None`)
    - Source assertion: no `logger.` call or exception in main.py interpolates the api_key value
    - Source assertion: main.py assigns a named `http_client = httpx.Client(timeout=10.0)` local and passes it as `client=http_client` to TrackingMoreRegistrar (no inline `httpx.Client(...)` inside the TrackingMoreRegistrar(...) constructor call)
    - `grep -n "http_client.close()" shipping_tracker/main.py` matches — the named httpx.Client is closed in finally
  </acceptance_criteria>
  <done>db.register_and_persist threads carrier through; main() fails fast on missing key, constructs a named http_client, wires the real TrackingMoreRegistrar against it, catches QuotaExceededError before the broad except and breaks, and closes http_client in finally; full Plan 01 suite GREEN with mypy --strict and ruff clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipping_tracker → TrackingMore API (HTTPS) | Outbound request carrying the API key in the Tracking-Api-Key header; response is untrusted input parsed in _handle |
| env (`.env`) → process | TRACKINGMORE_API_KEY enters the process; must never leave via logs or exceptions |
| parser output → registrar | tracking_number / carrier cross into the request body (already validated upstream by the parser) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | API key in logs/exceptions | mitigate | Key read from env only (D-05); `config.missing_api_key` logs no value; no log/exception in registrar.py or main.py interpolates `self._api_key` — enforced by `test_api_key_never_logged` and a source-grep acceptance criterion |
| T-05-04 | Information Disclosure | tracking_number / carrier in logs | mitigate | All registrar log lines and exception messages are structural only ("registrar.created", "rate-limit", "quota-exhausted"); WR-04 logs message_id + type only; enforced by `test_tracking_number_never_logged` |
| T-05-05 | Tampering | Malformed/non-JSON TrackingMore response | mitigate | Defensive `try resp.json() except → {}` (Pitfall 6, RESEARCH Q-2 RESOLUTION); unknown meta.code falls through to status checks → transient retry or return False (no persist) rather than crashing |
| T-05-06 | Denial of Service | Hung socket stalls cron job | mitigate | 10s httpx timeout (D-03) + single ~2s retry caps a dead endpoint at ~22s, then defers (D-02); named http_client closed in finally (Pitfall 4) |
| T-05-07 | Tampering | SSRF via tracking_number | accept | URL is the hardcoded `_BASE_URL` constant, never constructed from input — no SSRF surface (RESEARCH Security Domain) |
| T-05-SC | Tampering | httpx/respx supply chain | accept | Both audited `[OK]` in 05-RESEARCH.md Package Legitimacy Audit; httpx already in-tree; no `[ASSUMED]`/`[SUS]` packages — no human checkpoint required |
</threat_model>

<verification>
- `pytest tests/test_registrar.py -q` — all 15 Plan 01 contract tests GREEN
- `pytest -x -q` — full suite GREEN (no regression in Phase 1-4 tests)
- `mypy --strict shipping_tracker/` — 0 errors (TrackingMoreRegistrar, QuotaExceededError, carrier passthrough fully typed)
- `ruff check .` — 0 violations
- Source review: `except QuotaExceededError` precedes `except Exception` in main.py; D-05 key check precedes any I/O; named http_client closed in finally; no PII/key in logs
</verification>

<success_criteria>
- TrackingMoreRegistrar registers via POST /v4/trackings/create and persists only on confirmed success (TRACK-01)
- API key sourced exclusively from TRACKINGMORE_API_KEY, never logged; missing key → main() returns 1 before any work (TRACK-02, D-05)
- 4016 already-exists treated as success (TRACK-03)
- 429/4021 → QuotaExceededError → main() breaks before broad except; remaining numbers retry next run (TRACK-04, D-01, D-06)
- 5xx/timeout → one retry then defer, not persisted (TRACK-04, D-02)
- carrier=None omits courier_code; carrier passed only as optional hint; registration never requires it (TRACK-05, D-08)
- main() owns a named http_client and closes it in finally (Pitfall 4)
- mypy --strict and ruff clean; full suite GREEN
</success_criteria>

<output>
Create `.planning/phases/05-pipeline/05-02-SUMMARY.md` when done
</output>
