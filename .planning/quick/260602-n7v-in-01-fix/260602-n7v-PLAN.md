---
phase: quick-260602-n7v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/test_registrar.py]
autonomous: true
requirements: [IN-01]
must_haves:
  truths:
    - "meta_code 4101 is exercised: treated as already-exists → registrar returns True"
    - "meta_code 4190 is exercised: → raises QuotaExceededError"
    - "HTTP status 402 is exercised: → raises QuotaExceededError (status-based quota trigger)"
    - "tests have teeth: removing any of these codes from _handle makes the corresponding case fail"
    - "test-only change; ruff/mypy (whole project)/pytest all pass"
  artifacts:
    - path: "tests/test_registrar.py"
      provides: "Parametrized coverage for 4101 / 4190 / HTTP-402 in _handle"
      contains: "4101"
  key_links:
    - from: "tests/test_registrar.py quota/already-exists parametrization"
      to: "shipping_tracker/registrar.py _handle meta_code branches"
      via: "behavior pin for previously-untested codes"
      pattern: "4190"
---

<objective>
Fix IN-01 (Phase 05 review): _handle's already-exists branch handles meta_code
(4016, 4101) and the quota branch handles (4021, 4190) or status 402, but the test
suite only exercises 4016 and 4021. The 4101 and 4190 mappings and the HTTP-402
quota trigger are unverified, so a future refactor could silently break them.

This is a pure test-coverage gap — no source change. Add parametrized cases.

Purpose: pin the previously-untested response-code branches so a refactor can't
silently drop them.
Output: parametrized tests in tests/test_registrar.py covering 4101, 4190, HTTP-402.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@shipping_tracker/registrar.py
@.planning/phases/05-pipeline/05-REVIEW.md
@tests/test_registrar.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add parametrized coverage for 4101 / 4190 / HTTP-402</name>
  <files>tests/test_registrar.py</files>
  <action>
Add two parametrized tests to tests/test_registrar.py (no source changes):

1. Already-exists codes return True — parametrize over meta_code in (4016, 4101),
   HTTP 400 body {"meta": {"code": <code>, ...}}. Assert registrar(FAKE_TRACKING_
   NUMBER_1, None) is True. (4016 is already covered elsewhere; including it here
   confirms the parametrization is correct and pins both members of the branch.)

2. Quota triggers raise QuotaExceededError — parametrize over (status_code,
   meta_code): (400, 4021), (400, 4190), (402, 200). The (402, 200) case proves the
   status-based trigger fires regardless of meta_code. Assert
   pytest.raises(QuotaExceededError) around registrar(FAKE_TRACKING_NUMBER_1, None).

Use the existing _make_registrar(mock_router) helper and the _CREATE_URL / mock_router
patterns already in the file. Keep response JSON message strings short (<= 88 cols,
ruff E501). No PII in any literal (synthetic only).
  </action>
  <verify>
    <automated>cd C:\Projects\shipping-tracker; python -m ruff check .; python -m mypy .; python -m pytest -q</automated>
  </verify>
  <done>
4101 (already-exists→True), 4190 (→QuotaExceededError), and HTTP-402 (→Quota
ExceededError) are each exercised by a parametrized case. ruff/mypy (whole project)/
pytest all green. Teeth confirmed: removing a code from _handle fails its case.
  </done>
</task>

</tasks>

<verification>
- `git diff` touches only tests/test_registrar.py.
- Teeth check: temporarily removing 4101 (or 4190, or the `== 402`) from _handle makes the matching case fail; restored after.
- `python -m ruff check .`, `python -m mypy .`, `python -m pytest -q` all pass.
</verification>

<success_criteria>
- 4101, 4190, and HTTP-402 each have an exercising test case.
- Tests pin the branches (have teeth) and are PII-free.
- Test-only change; full lint/type/test suite green.
</success_criteria>

<output>
Create `.planning/quick/260602-n7v-in-01-fix/260602-n7v-SUMMARY.md` when done.
</output>
