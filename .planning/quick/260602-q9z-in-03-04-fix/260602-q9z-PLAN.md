---
phase: quick-260602-q9z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/conftest.py]
autonomous: true
requirements: [IN-03, IN-04]
must_haves:
  truths:
    - "the five unused TrackingMore response builders are removed from conftest.py (IN-03)"
    - "the unused synthetic_email_body fixture is removed from conftest.py (IN-04)"
    - "the now-unused httpx import (only the builders used it) is removed"
    - "the used fixtures db_conn and mock_router are preserved unchanged"
    - "test-only change; ruff/mypy (whole project)/pytest all pass with the same test count (86)"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Conftest trimmed to only the fixtures actually consumed"
      contains: "def mock_router"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/test_registrar.py inline response objects"
      via: "inline form is the established convention; dead duplicates removed"
      pattern: "httpx.Response"
---

<objective>
Fix IN-03 and IN-04 (Phase 05 review) — both test-hygiene cleanups in tests/conftest.py.

IN-03: make_success_response / make_already_exists_response / make_quota_response /
make_rate_limit_response / make_5xx_response are module-level builders that no test
imports — test_registrar.py builds its httpx.Response objects inline. Dead/duplicated
code that can drift.

IN-04: the synthetic_email_body fixture is defined but consumed by no test.

Confirmed via grep across the whole tests/ tree: ZERO references to any of the five
builders or synthetic_email_body outside conftest.py itself. (db_conn and mock_router
ARE used and must be preserved.)

DECISION: delete the dead code (the inline-response form is the established
convention — the review's own "if the inline form is preferred" branch). Also remove
the now-unused `import httpx` (only the builders referenced it).

Purpose: remove dead/duplicated test scaffolding so the canonical synthetic
responses live in exactly one place (inline) and can't drift.
Output: trimmed conftest.py. No behavior change; test count unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@tests/conftest.py
@.planning/phases/05-pipeline/05-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused builders + synthetic_email_body fixture + httpx import</name>
  <files>tests/conftest.py</files>
  <action>
In tests/conftest.py:
1. Delete the `synthetic_email_body` fixture (IN-04).
2. Delete the entire "Synthetic TrackingMore v4 response builders" comment block
   and all five builder functions: make_success_response, make_already_exists_
   response, make_quota_response, make_rate_limit_response, make_5xx_response (IN-03).
3. Remove `import httpx` (it was used ONLY by the deleted builders).
4. PRESERVE unchanged: the module docstring, `import sqlite3`,
   `from collections.abc import Generator`, `import pytest`, `import respx`,
   `from shipping_tracker.db import init_db`, and the `db_conn` and `mock_router`
   fixtures (both are consumed by tests).

Do not touch any other test file — test_registrar.py already builds its responses
inline, which is the retained canonical form.
  </action>
  <verify>
    <automated>cd C:\Projects\shipping-tracker; python -m ruff check .; python -m mypy .; python -m pytest -q</automated>
  </verify>
  <done>
conftest.py contains only the consumed fixtures (db_conn, mock_router) plus their
imports; the five unused builders, synthetic_email_body, and the httpx import are
gone. ruff/mypy (whole project)/pytest all green with the same 86-test count
(nothing depended on the removed code).
  </done>
</task>

</tasks>

<verification>
- `git diff tests/conftest.py` shows only deletions (fixture, builders, httpx import).
- `python -m ruff check .` passes (no unused-import or other warnings).
- `python -m mypy .` passes.
- `python -m pytest -q` passes with 86 tests (unchanged) — proves nothing consumed the removed code.
</verification>

<success_criteria>
- IN-03: the five unused response builders removed.
- IN-04: the unused synthetic_email_body fixture removed.
- Unused httpx import removed; db_conn / mock_router preserved.
- Test-only change; full lint/type/test suite green, test count unchanged.
</success_criteria>

<output>
Create `.planning/quick/260602-q9z-in-03-04-fix/260602-q9z-SUMMARY.md` when done.
</output>
