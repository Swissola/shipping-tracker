---
phase: quick
plan: 260601-pa7
type: execute
wave: 1
depends_on: []
files_modified:
  - shipping_tracker/db.py
  - tests/test_db.py
autonomous: true
requirements: [WR-01]

must_haves:
  truths:
    - "Calling register_and_persist twice for the same already-present rows is a silent no-op (no IntegrityError)"
    - "register_and_persist still returns True on a successful (idempotent) registrar call"
    - "All 57 existing db tests stay green"
    - "register_and_persist's docstring documents that it is idempotent / safe to retry"
  artifacts:
    - path: "shipping_tracker/db.py"
      provides: "register_and_persist using INSERT OR IGNORE for both row writes, with idempotency docstring note"
      contains: "INSERT OR IGNORE"
    - path: "tests/test_db.py"
      provides: "Test asserting register_and_persist is a silent no-op on a second call for already-present rows"
      contains: "register_and_persist"
  key_links:
    - from: "shipping_tracker/db.py register_and_persist"
      to: "INSERT OR IGNORE semantics (matches main.py DEDUP-04 branch)"
      via: "both INSERT statements inside the `with conn:` transaction"
      pattern: "INSERT OR IGNORE"
---

<objective>
Make `register_and_persist` in `shipping_tracker/db.py` self-defending against
direct calls (code review finding WR-01). Both row writes currently use a bare
`INSERT`, which raises `sqlite3.IntegrityError` (PRIMARY KEY violation) if the
function is called without the upstream dedup guard having run first. Change both
`INSERT` statements to `INSERT OR IGNORE` so the function is idempotent and
consistent with the `INSERT OR IGNORE` already used in `main.py`'s DEDUP-04
mark-processed branch.

Purpose: Close WR-01 — eliminate the non-reentrancy that makes the function rely
entirely on the caller having checked dedup first, and remove the disagreement
between the two `processed_emails` write paths.

Output: An idempotent, retry-safe `register_and_persist` with a documented
contract, plus a regression test proving a repeated call is a silent no-op. All
existing tests remain green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@shipping_tracker/db.py
@shipping_tracker/main.py
@tests/test_db.py
@.planning/phases/04-deduplication/04-REVIEW.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make both register_and_persist writes idempotent and add a no-op regression test</name>
  <files>shipping_tracker/db.py, tests/test_db.py</files>
  <behavior>
    - New test: calling register_and_persist twice with success_registrar for the
      SAME message_id + tracking_number does not raise IntegrityError; the second
      call returns True and the rows remain present (count stays 1 per table).
    - Existing contracts unchanged: test_register_and_persist_success,
      test_register_and_persist_fail_returns_false,
      test_register_and_persist_raises_rolls_back, and test_retry_proof all stay
      green (fail/raise paths still write neither row; atomic rollback preserved).
  </behavior>
  <action>
    In shipping_tracker/db.py, inside register_and_persist's `with conn:` block,
    change BOTH bare INSERT statements to INSERT OR IGNORE so duplicate primary
    keys are silently ignored rather than raising IntegrityError: the
    processed_emails write becomes "INSERT OR IGNORE INTO processed_emails VALUES
    (?, ?)" and the registered_tracking write becomes "INSERT OR IGNORE INTO
    registered_tracking VALUES (?, ?, ?, NULL, NULL)". This matches the
    INSERT OR IGNORE pattern already used in main.py's DEDUP-04 branch (per WR-01).

    Preserve everything else exactly: keep the single `with conn:` transaction
    (atomic two-row write — commit on exit, rollback on exception, D-01), keep the
    registrar call and exception re-raise ahead of the transaction (D-08), keep the
    `if not success: return False` early return, and keep the function signature
    and the LOG-02 privacy contract (message_id only, never tracking_number)
    unchanged. Do NOT touch main.py or any call site.

    Add a note to register_and_persist's docstring stating the function is now
    idempotent / safe to retry — a repeat call for an already-present message_id
    or tracking_number is a silent no-op (INSERT OR IGNORE) and still returns True,
    rather than raising IntegrityError. Keep the existing docstring lines about
    return semantics, exception propagation, and LOG-02.

    In tests/test_db.py, add a test (e.g. test_register_and_persist_idempotent)
    under the DEDUP-05 section. It must use only synthetic FAKE-prefixed fixtures
    (FAKE_MESSAGE_ID_1, FAKE_TRACKING_NUMBER_1) and the existing success_registrar.
    Call register_and_persist once (assert True, both rows present), then call it a
    SECOND time with the identical message_id + tracking_number and assert: it does
    NOT raise (no IntegrityError), it returns True, and each table still holds
    exactly one matching row (SELECT COUNT(*) == 1) — proving the silent no-op.
    Do not weaken or alter the existing test_retry_proof or the other
    register_and_persist tests.
  </action>
  <verify>
    <automated>python -m pytest tests/test_db.py -q</automated>
  </verify>
  <done>
    Both INSERT statements in register_and_persist read INSERT OR IGNORE; the
    docstring documents idempotency; the new no-op test passes; all previously
    existing tests in tests/test_db.py still pass (57 total green); no changes to
    main.py or any public signature.
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_db.py -q` — all tests pass (existing 57 + the new no-op test).
- `python -m ruff check shipping_tracker/db.py tests/test_db.py` — clean.
- `python -m mypy shipping_tracker/db.py` — clean.
- Manual grep: `grep -c "INSERT OR IGNORE" shipping_tracker/db.py` returns 2 (both writes converted).
</verification>

<success_criteria>
- WR-01 closed: register_and_persist is reentrant — a duplicate call for
  already-present rows is a silent no-op (no IntegrityError) and returns True.
- Both row writes use INSERT OR IGNORE, consistent with main.py's DEDUP-04 branch.
- The atomic two-row `with conn:` transaction semantics are preserved
  (fail/raise paths still write neither row; test_retry_proof unchanged and green).
- Docstring documents the idempotent / safe-to-retry contract.
- Privacy (LOG-02) unchanged; only synthetic fixtures used in the new test.
- No changes to db.py public signatures or main.py call sites.
</success_criteria>

<output>
Create `.planning/quick/260601-pa7-make-register-and-persist-in-shipping-tr/260601-pa7-SUMMARY.md` when done
</output>
