---
phase: quick-260602-j4p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [shipping_tracker/main.py, tests/test_main.py]
autonomous: true
requirements: [WR-04]
must_haves:
  truths:
    - "the missing-credentials branch logs only the basename of the credentials path, never the full path"
    - "an absolute credentials path embedding a home-dir username (e.g. /home/<user>/.../credentials.json) leaks no username/directory to the log"
    - "branch still returns 1 and logs a gmail.credentials.missing record (PII-safe)"
    - "ruff/mypy --strict/pytest all pass; regression test proves the full path / username is not logged"
  artifacts:
    - path: "shipping_tracker/main.py"
      provides: "PII-safe missing-credentials log (basename only)"
      contains: "os.path.basename"
    - path: "tests/test_main.py"
      provides: "Regression test for WR-04 (no path/username leak)"
      contains: "credentials.missing"
  key_links:
    - from: "shipping_tracker/main.py missing-credentials log"
      to: "no-PII-in-logs constraint (LOG-02 / project privacy constraint)"
      via: "log basename, not full path"
      pattern: "os.path.basename"
---

<objective>
Fix WR-04 (Phase 05 review): the missing-credentials branch at shipping_tracker/main.py:105
logs the full credentials file path —
`logger.error("gmail.credentials.missing path=%s", exc.filename)`. The default
(`credentials.json`) is harmless, but operators commonly point GMAIL_CREDENTIALS_PATH at
an absolute path like `/home/<username>/.config/shipping-tracker/credentials.json`, so the
log can embed an OS username. Given the project's non-negotiable "no PII in logs" constraint
and intended public release, this is a privacy regression.

Fix: log only the basename (or a structural marker), never the full path.

NOTE: This is the Phase-05-review WR-04 (credentials-path leak), distinct from the Phase-03
WR-04 dispatch-error hardening already committed as 1c5b347 and recorded by quick task
260601-kp7. They are separate findings that happen to share a number.

Purpose: Remove the username/path leak from the missing-credentials log.
Output: PII-safe log line in main() + a regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@shipping_tracker/main.py
@.planning/phases/05-pipeline/05-REVIEW.md
@tests/test_aliexpress_parser.py
@tests/test_main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Log only the credentials basename + add regression test</name>
  <files>shipping_tracker/main.py, tests/test_main.py</files>
  <action>
In shipping_tracker/main.py, replace the missing-credentials log (currently line 105):

    logger.error("gmail.credentials.missing path=%s", exc.filename)

with a basename-only form that cannot leak a directory/username:

    logger.error(
        "gmail.credentials.missing name=%s",
        os.path.basename(exc.filename or ""),
    )

`os` is already imported. Leave the `return 1` and surrounding try/except unchanged.

Add a regression test to tests/test_main.py (mirror the PII-safe pattern in
tests/test_aliexpress_parser.py:310 — stub configure_logging to no-op so caplog
captures, set a synthetic TRACKINGMORE_API_KEY and DATABASE_PATH=:memory:, isolate
CWD with monkeypatch.chdir(tmp_path)). The test MUST:
- Patch shipping_tracker.main.fetch_unread_shipping_emails to raise a
  FileNotFoundError whose .filename is an absolute path embedding a fake username,
  e.g. "/home/SECRETUSER/.config/shipping-tracker/credentials.json".
- Assert main() returns 1.
- Assert a "gmail.credentials.missing" record exists and that across ALL caplog
  records the rendered messages contain "credentials.json" (the basename) but NOT
  "SECRETUSER" and NOT the directory portion (e.g. "/home/" or ".config").
  </action>
  <verify>
    <automated>cd C:\Projects\shipping-tracker; python -m ruff check .; python -m mypy --strict shipping_tracker/; python -m pytest -q</automated>
  </verify>
  <done>
The missing-credentials branch logs only os.path.basename(exc.filename), never the
full path. The regression test drives main() with a FileNotFoundError carrying an
absolute username-bearing path and asserts the username/directory never reaches the
log while the basename does. ruff/mypy --strict/pytest all green.
  </done>
</task>

</tasks>

<verification>
- `git diff shipping_tracker/main.py` shows only the missing-credentials log line changed.
- New test fails against old code (full path/username logged), passes against fix.
- `python -m ruff check .`, `python -m mypy --strict shipping_tracker/`, `python -m pytest -q` all pass.
</verification>

<success_criteria>
- Missing-credentials log emits basename only; no path/username leak.
- Branch still returns 1 with a PII-safe gmail.credentials.missing record.
- Regression test in place; full lint/type/test suite green.
</success_criteria>

<output>
Create `.planning/quick/260602-j4p-wr-04-fix/260602-j4p-SUMMARY.md` when done.
</output>
