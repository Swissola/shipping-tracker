---
phase: quick-260602-p8x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [shipping_tracker/registrar.py]
autonomous: true
requirements: [IN-02]
must_haves:
  truths:
    - "the post-loop dead line is an explicit raise AssertionError('unreachable'), not a silent return False that could be mistaken for a reachable fallthrough"
    - "mypy's return-path analysis is still satisfied (raise terminates, all in-loop paths return bool)"
    - "no behavior change: the line remains unreachable; all existing tests pass unchanged"
    - "ruff/mypy (whole project)/pytest all pass"
  artifacts:
    - path: "shipping_tracker/registrar.py"
      provides: "Explicit unreachable marker after the retry loop"
      contains: "raise AssertionError"
  key_links:
    - from: "shipping_tracker/registrar.py __call__ post-loop line"
      to: "mypy return-path analysis"
      via: "raise terminates the function, satisfying the type checker"
      pattern: "unreachable"
---

<objective>
Fix IN-02 (Phase 05 review): the final line of __call__,
`return False  # unreachable; mypy requires it`, is dead code that only exists to
satisfy mypy's return-path analysis. The comment is accurate (every loop iteration
returns on all paths), but a bare `return False` can be mistaken for a reachable
fallthrough during future edits.

Fix: replace it with `raise AssertionError("unreachable")` to make the intent
explicit while still satisfying mypy.

Purpose: make the unreachable intent unmistakable; avoid a silent wrong-looking
fallthrough.
Output: explicit unreachable marker in registrar.py. No behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@shipping_tracker/registrar.py
@.planning/phases/05-pipeline/05-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the unreachable return False with an explicit raise</name>
  <files>shipping_tracker/registrar.py</files>
  <action>
In shipping_tracker/registrar.py, in __call__, replace the post-loop line:

    return False  # unreachable; mypy requires it

with:

    # Unreachable: both attempts in the loop above return on every path. The
    # explicit raise (vs a silent `return False`) makes that intent clear and
    # still satisfies mypy's return-path analysis (IN-02).
    raise AssertionError("unreachable")

Do not touch any other line. No test is added: the statement is provably
unreachable, so it cannot be exercised; correctness is proven by mypy accepting
the function and all existing tests passing unchanged.
  </action>
  <verify>
    <automated>cd C:\Projects\shipping-tracker; python -m ruff check .; python -m mypy .; python -m pytest -q</automated>
  </verify>
  <done>
The dead `return False` is replaced by `raise AssertionError("unreachable")`.
mypy still accepts the function (return-path analysis satisfied by the raise) and
all existing tests pass unchanged. ruff/mypy (whole project)/pytest all green.
  </done>
</task>

</tasks>

<verification>
- `git diff shipping_tracker/registrar.py` shows only the single post-loop line changed.
- `python -m ruff check .`, `python -m mypy .`, `python -m pytest -q` all pass.
</verification>

<success_criteria>
- Post-loop line is an explicit `raise AssertionError("unreachable")`.
- mypy return-path analysis satisfied; no behavior change; all tests green.
</success_criteria>

<output>
Create `.planning/quick/260602-p8x-in-02-fix/260602-p8x-SUMMARY.md` when done.
</output>
