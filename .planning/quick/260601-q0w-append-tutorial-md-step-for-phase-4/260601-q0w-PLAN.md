---
phase: quick
plan: 260601-q0w
type: execute
wave: 1
depends_on: []
files_modified: [TUTORIAL.md]
autonomous: true
requirements: [TUTORIAL-MAINTENANCE]
must_haves:
  truths:
    - "TUTORIAL.md contains a new '## Step 5 — Phase 4: Deduplication' section after the existing Step 4"
    - "Everything above the insertion point (Steps 0-4, intro, GSD loop) is preserved unchanged"
    - "The new section mirrors Step 4's sub-structure: discuss/research → plan & execute the 3 waves → course-correction subsection → verify → bold Teaching point"
    - "All commit hashes cited are the real hashes from the Phase 4 SUMMARY files and the pa7 quick task"
    - "The WR-01 code-review course-correction (bare INSERT vs INSERT OR IGNORE, fixed via quick task pa7) is framed as the instructive moment"
    - "The honest incomplete-pipeline state (NullRegistrar seam, Phase 5 drop-in) and the one pending human-UAT item are noted"
    - "The 'Where we are now' status section is updated to reflect Phase 4 complete"
  artifacts:
    - path: "TUTORIAL.md"
      provides: "Step 5 — Phase 4 teaching narrative"
      contains: "## Step 5 — Phase 4: Deduplication"
  key_links:
    - from: "TUTORIAL.md Step 5"
      to: ".planning/phases/04-deduplication/ artifacts"
      via: "relative markdown links matching Step 4 link style"
      pattern: "\\.planning/phases/04-deduplication"
---

<objective>
Append a new "## Step 5 — Phase 4: Deduplication" section to TUTORIAL.md, following the
project's tutorial-maintenance convention (CLAUDE.md §Conventions). Phase 4 has passed its
verify beat (4/4 must-haves, human_needed for one live-run UAT item), so a new teaching step
is now due before advancing to Phase 5.1.

Purpose: keep TUTORIAL.md current as a GSD teaching walkthrough — each phase gets a step
sourced from its real artifacts, with real commit hashes, absolute dates, the course-correction
called out explicitly, and a closing Teaching point.

Output: TUTORIAL.md with a new Step 5 section inserted after Step 4 and the "Where we are now"
status block updated. No code or other docs touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@TUTORIAL.md
@.planning/phases/04-deduplication/04-CONTEXT.md
@.planning/phases/04-deduplication/04-RESEARCH.md
@.planning/phases/04-deduplication/04-01-SUMMARY.md
@.planning/phases/04-deduplication/04-02-SUMMARY.md
@.planning/phases/04-deduplication/04-03-SUMMARY.md
@.planning/phases/04-deduplication/04-REVIEW.md
@.planning/phases/04-deduplication/04-VERIFICATION.md
@.planning/quick/260601-pa7-make-register-and-persist-in-shipping-tr/260601-pa7-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Step 5 (Phase 4: Deduplication) to TUTORIAL.md and update the status block</name>
  <files>TUTORIAL.md</files>
  <action>
Insert a new "## Step 5 — Phase 4: Deduplication" section into TUTORIAL.md immediately AFTER
the existing Step 4 section (which ends just before the "---" preceding "## Where we are now",
around line 305) and BEFORE that "## Where we are now" block. Preserve everything above the
insertion point byte-for-byte: the intro blockquote, "What you're looking at", "The GSD loop",
and Steps 0 through 4. Do NOT renumber existing steps. Do NOT touch the Appendix.

Mirror Step 4's sub-structure exactly. Step 4 used: a bold "**Goal:**" line; "### 4a. Discuss &
research"; "### 4b. Plan & execute — three waves"; "### 4c. The course-correction: ..." with a
⚠️ marker; "### 4d. Verify, secure & a follow-up"; and a closing bold "**Teaching point — ...**".
Use the same heading style for Step 5: "### 5a", "### 5b", "### 5c", "### 5d". Keep the prose
tone, paragraph density, and inline-link style of Step 4 (relative links into
`.planning/phases/04-deduplication/...`). Dates are absolute: **2026-06-01**.

Source the narrative ONLY from the read artifacts (NOT from memory). Required content:

GOAL line (from ROADMAP/CONTEXT/VERIFICATION): the SQLite state layer that makes the whole
pipeline idempotent — an email is never re-processed, a tracking number is never re-registered,
and a *failed* registration is retried automatically on the next run. Two tables
(`processed_emails`, `registered_tracking`) with schemas locked by DEDUP-01/DEDUP-02.

5a. Discuss & research (from 04-CONTEXT.md + 04-RESEARCH.md): note the discussion locked the
write-timing retry core — both rows written together in ONE transaction, only on confirmed
registrar success, so a failure writes neither row and the number retries next run (D-01);
no-tracking / no-match emails left UNMARKED so an improved parser can re-read them (D-02);
the duplicate-notification branch marks the email processed to avoid re-parse churn (D-03);
state layer as a module of plain functions in `db.py` with the connection passed in, not a
class (D-04); one connection per run, opened in `main()` and closed in `finally` (D-05); schema
built exactly to DEDUP-01/02 with no speculative `provider` column (D-06, deferred to a v2
`ALTER TABLE`); `PRAGMA busy_timeout = 5000` + `PRAGMA user_version = 1` (D-10/D-11). The key
architectural move: an INJECTABLE registrar seam (D-08) with a `NullRegistrar` placeholder
(D-09) so DEDUP-05's retry guarantee is provable in Phase 4 WITHOUT the real TrackingMore client.
Research (stdlib-only, no new dependencies) verified the `with conn:` atomic two-row write, the
`typing.Protocol` Registrar contract, and ran a retry-proof probe before any code was written.

5b. Plan & execute — three waves (from the three SUMMARY files; use the REAL commit hashes):
  - Wave 0 / Plan 04-01 (Nyquist RED scaffold): 15 failing DEDUP test functions, FAKE-prefixed
    fixtures (`fake_db.py`), and an in-memory `db_conn` fixture — tests authored before the
    source modules exist. Commits `fad212a`, `d54951d`.
  - Wave 1 / Plan 04-02 (the state layer): `db.py` (`init_db`, `is_email_processed`,
    `is_tracking_registered`, `register_and_persist`) + `registrar.py` (the `Registrar`
    Protocol + `NullRegistrar`). Atomic `with conn:` two-row write turns all 15 tests GREEN.
    Commit `7b886c0`.
  - Wave 3 / Plan 04-03 (main wiring): connection lifecycle in `main()`, the `NullRegistrar`
    seam injection, and the DEDUP-03 (before parse) / DEDUP-04 (after parse, INSERT OR IGNORE
    mark-processed) / DEDUP-05 (register-then-persist) checks wired into the dispatch loop;
    `DATABASE_PATH` added to `.env.example`, `data/` to `.gitignore`. Commits `f36566b`,
    `eb62b96`.

5c. The course-correction (⚠️ — the instructive moment, the Step-4-equivalent): frame WR-01.
After execution all 15 DEDUP tests (57/57 full suite) were green and verification scored 4/4.
The automated `/gsd-code-review` gate (04-REVIEW.md) still flagged WR-01: `register_and_persist`
wrote both rows with a bare, non-idempotent `INSERT`, while the DEDUP-04 mark-processed branch
in `main.py` used `INSERT OR IGNORE`. The two write paths to the same table DISAGREED on
idempotency. This is a latent self-undercut: the function at the very heart of the phase's retry
guarantee was itself not retry-safe — a future caller (or a crash-window re-registration) hitting
an already-present row would raise `IntegrityError` mid-transaction. The verifier assessed it as
a WARNING (it did not break SC4 as wired, because the upstream guard prevented the bad call in
the normal path) and recorded it for a follow-up. It was fixed via quick task `260601-pa7`:
both `INSERT` statements became `INSERT OR IGNORE`, aligning the two paths, with a no-op
regression test proving a repeat call is a silent success. Commit `5a58eaf`.

5d. Verify & the honest deferred state (from 04-VERIFICATION.md): goal-backward verification
confirmed 4/4 observable truths and all 5 DEDUP requirements (DEDUP-01..05) true against the
code — including the retry proof (`test_retry_proof`: fail-run leaves zero rows, success-run
writes both). Be explicit and honest about the incomplete-pipeline state: a real Phase 4 cron
run creates the tables and exercises the skip paths but `NullRegistrar` always returns False, so
`registered_tracking` stays empty — Phase 5 drops the real `TrackingMoreRegistrar` into the same
seam with ZERO changes to `db.py` or the dispatch loop. Note the one pending human-UAT item: a
live `NullRegistrar` cron run against a real Gmail OAuth token + populated `.env` (verifier
status `human_needed`), which in-process pytest cannot exercise.

Teaching point (bold, closing): tie it to WR-01 — green tests + a passing goal-verification still
left a write path that quietly undercut the phase's own retry guarantee; the independent code-
review gate caught the inconsistency and a bounded follow-up quick task closed it. The lesson
parallels Step 4's: GSD runs code review as a separate gate AFTER execution, and an injectable
seam (NullRegistrar) lets a phase PROVE its core guarantee before the real external dependency
exists — an honest "incomplete but verified" state beats a half-wired loop.

Finally, update the "## Where we are now" block: change the Phase 4 line from the "⏭️ Next"
entry to a "✅ **Phase 4 — Deduplication:** complete and verified" entry (4/4 must-haves; note
the WR-01 review catch closed via quick task pa7; one human-UAT live-run item pending), and set
the next-up line to Phase 5 / 5.1. Update the milestone progress line to "4 of 8 phases (50%)"
to match STATE.md.

Do NOT place any fenced code blocks that change Steps 0-4. Keep the new section prose-and-link
only, matching Step 4 (Step 4 uses inline code spans and links, not fenced blocks).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content -Raw TUTORIAL.md; if ($c -notmatch '## Step 5 . Phase 4: Deduplication') { throw 'Step 5 heading missing' }; foreach ($h in 'fad212a','d54951d','7b886c0','f36566b','eb62b96','5a58eaf') { if ($c -notmatch $h) { throw \"missing hash $h\" } }; foreach ($s in '## Step 4 . Phase 3: Parser Layer','WR-01','NullRegistrar','2026-06-01','Teaching point') { if ($c -notmatch [regex]::Escape($s)) { throw \"missing token $s\" } }; $i5 = $c.IndexOf('## Step 5'); $i4 = $c.IndexOf('## Step 4'); $iw = $c.IndexOf('## Where we are now'); if (-not ($i4 -lt $i5 -and $i5 -lt $iw)) { throw 'Step 5 not positioned between Step 4 and Where-we-are-now' }; Write-Output 'OK'"</automated>
  </verify>
  <done>
TUTORIAL.md contains "## Step 5 — Phase 4: Deduplication" positioned after Step 4 and before
"## Where we are now"; all six real commit hashes (fad212a, d54951d, 7b886c0, f36566b, eb62b96,
5a58eaf) appear; WR-01, NullRegistrar, the 2026-06-01 date, and a bold Teaching point are present;
Steps 0-4 and the Appendix are unchanged; the status block reflects Phase 4 complete (4 of 8, 50%).
  </done>
</task>

</tasks>

<verification>
- `## Step 5 — Phase 4: Deduplication` exists and sits between Step 4 and "Where we are now".
- The five sub-sections (5a discuss/research, 5b three waves, 5c WR-01 course-correction,
  5d verify & deferred state, closing Teaching point) are present and mirror Step 4's structure.
- Real commit hashes only — fad212a / d54951d (04-01), 7b886c0 (04-02), f36566b / eb62b96 (04-03),
  5a58eaf (pa7 WR-01 fix). No invented hashes.
- Course-correction framed as WR-01: bare INSERT vs INSERT OR IGNORE, the self-undercut of the
  retry guarantee, fixed via quick task 260601-pa7.
- Honest deferred state (NullRegistrar / Phase 5 drop-in) and the one human-UAT item are noted.
- Links into `.planning/phases/04-deduplication/...` are relative, matching Step 4's link style.
- Steps 0-4 and the Appendix unchanged; only TUTORIAL.md modified.
</verification>

<success_criteria>
- TUTORIAL.md teaches Phase 4 as Step 5, sourced from artifacts (not memory), in the established
  discuss → plan/execute → course-correction → verify → Teaching-point format.
- The WR-01 course-correction is the instructive centerpiece with the real pa7 fix commit.
- The "Where we are now" status block shows Phase 4 complete and 4 of 8 phases (50%).
- No code or non-TUTORIAL docs are modified.
</success_criteria>

<output>
Create `.planning/quick/260601-q0w-append-tutorial-md-step-for-phase-4/260601-q0w-SUMMARY.md` when done.
</output>
